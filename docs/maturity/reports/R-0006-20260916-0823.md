# Hextory maturity pulse — R-0006

| Field | Value |
|---|---|
| **Report ID** | R-0006 |
| **Date / time** | 2026-09-16 08:23 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse) |
| **Repo path** | `/Users/mblazquez/Documents/dev/pixpod/hextory` |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0005](R-0005-20260915-1923.md) overall **35.3** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **53.0** |
| **Band** | Building foundations (40–69) — **first-slice scaffold** |
| **Meets ≥90?** | **NO** |
| **One-line summary** | DES-0002 first slice is on disk (`src/` + `adapters/local` + `tests/`; **pytest 31 passed**); Gatekeeper/Q-GATE-1 fail-closed runtime exists; DES-0003 remains Draft ideation only; still **no `.git` / CONTRIBUTING / CI**. |

**Biggest blocker to 90+:** Public git + CONTRIBUTING + CI design-gate automation are still absent — first-slice scaffold lifts architecture/testability/traceability but cannot alone clear the ≥90 bar while aspects 7/10 stay low.

Δ vs R-0005: **+17.7** (35.3 → 53.0). Material disk progress since authorized-but-unstarted state at R-0005.

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0005 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **65** | +5 | DES-0001 **Approved**; DES-0002 **Approved**; README status = first-slice scaffold; DES-0003 labeled **Draft ideation only** (no build auth); layout tree shows 0001/0002 Approved | Outsider dry-run; scrub leftover “Draft” / “may change in review” in Approved DES-0002 body (~+25) |
| 2 | Design-doc coverage | **60** | +5 | TEMPLATE + Approved vision + Approved factory-engine + **DES-0003 Draft** ideation (`docs/design/0003-workflow-studio.md`); no other concrete workflow SDDs | More Approved workflow SDDs beyond engine/starter; do not count Draft Studio as coverage of UI impl (~+30) |
| 3 | Review rigor (dual human+agent) | **50** | 0 | Still two dual Approvals + agent records under `docs/design/reviews/`; DES-0003 reviewers **TBD** | Dual review on more SDDs (incl. Studio when ready) (~+40) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **65** | +25 | DES-0002 §12 + live IMPL under `src/` / `adapters/local/`; tests cite TEST-0010–0023 / AC-01–AC-12; pytest **31** green | Commit/PR DES citations once git exists; fuller REQ↔TEST matrix docs (~+25) |
| 5 | Architecture purity (hexagonal + open graphs) | **70** | +38 | Pure `src/` + ports; `adapters/local` only; TEST-0010 no adapter imports; `GraphRegistry` + TEST-0018 custom nodes without editing departments; no LangGraph in core (`src/graphs/runtime.py`) | CI import-linter; more workflows than starter (~+20) |
| 6 | Determinism, testability & readable core | **72** | +44 | `tests/{unit,behavior,adapters}`; GWT in behavior modules (TEST-0021); no Cucumber (TEST-0020); readable ~2k LOC core; MemorySaver via port | Formal CI TDD gate; scrub DES-0002-E “may change in review” residual (~+18) |
| 7 | Automation of design gates (CI/process) | **28** | +18 | Runtime `PolicyGatekeeper` + `LocalSddStatusReader` fail-closed; `config/sdd_status.json`; dual-source conflict → ERROR; **no `.git` / `.github` / branch protection** | Public git + CI SDD-citation + human merge-via-doc (~+62) |
| 8 | Factory observability (traces, quality loops) | **38** | +20 | Append-only `routing_history` (TEST-0013); rework/escalate paths (TEST-0015–0017); CLI prints status/routing (TEST-0019) | Persistent trace store + structured FAIL report artifact + dashboards (~+52) |
| 9 | Multi-target deploy readiness | **40** | +20 | `adapters/local` (CLI, MemorySaver, SddStatusReader) real; on-prem/AWS still deferred (DES-0002-J / NG1); only `adapters/local` on disk | Parity matrix + second target smoke (~+50) |
| 10 | Public project readiness | **42** | +2 | MIT `LICENSE`; README run/CLI docs accurate; DES-0003 Draft called out; **no CONTRIBUTING**, **no `.git`**, no issue/PR templates (git deferred by Marcos) | Hosting + CONTRIBUTING + templates (~+48) |

**Readable code bar (cross-cutting):** First-slice modules are walk-through-friendly (small ports, explicit Gatekeeper, registry without cleverness). Spot-check: `src/policies/gate.py`, `src/graphs/registry.py`, `adapters/local/sdd_status_reader.py`.

**Sum / overall:** 65+60+50+65+70+72+28+38+40+42 = **530** ÷ 10 = **53.0**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Partial → Stronger in practice** | Merge-via-doc-readiness still documented (README, TEST-0023). First slice implemented under Approved DES-0002 only. Still no real public merge practice (no git). Not treated as sole product goal — open graphs + Studio ideation also in view. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Strong (first-slice proven)** | `GraphRegistry` + TEST-0018 register custom prep→stamp without editing `src/departments/`. Starter three-node graph is a sample, not a closed catalog. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Strong / location in use** | Layout `tests/{unit,behavior,adapters}` live; GWT narrative in ordinary pytest; Cucumber/behave absent. Residual “Marcos may change in review” wording still in Approved DES-0002-E body — freeze hygiene remaining. |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Policy + runtime:** Gatekeeper denies missing/non-Approved; first slice matches Approved DES-0002-J. DES-0003 Draft → **no UI/React/xyflow code**. |
| Human merge approval = design-doc readiness (not code walkthrough) | **Documented** (README, DES-0001-B, DES-0002-D, TEST-0023); **unpracticed** without git merges. |
| Any merge/implementation without Approved SDD? | **No product UI/web**; engine slice authorized by Approved DES-0002. No `.git` on Mac SoT this run. |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Authorized first slice unstarted | High | **Mitigated** | `src/` + `adapters/local` + `tests/` present; pytest 31 passed — R-0005 blocker closed. |
| Pulse ritual without disk progress | Med | Mitigated this run | +17.7 overall with concrete paths; avoid treating flat pulses as progress later. |
| Stale “Draft” / “may change in review” inside Approved DES-0002 | Med | **Open** | DES-0002-E still “Marcos may change in review”; §15 “Decided in this Draft”; DigitalTraveler “Draft freeze” wording. README tree fixed for 0002. |
| DES-0003 Status cell prose breaks markdown parser | Med | **Open / fail-closed** | Status `**Draft** (ideation…)` → `parse_markdown_status` may miss; with manifest lacking DES-0003 → **UNKNOWN** (deny). Safe fail-closed today; would false-deny if Approved with parenthetical prose. |
| Manifest omits DES-0003 | Low | Open | `config/sdd_status.json` has DES-0001/0002/9999 only — intentional for Draft; document sync when Studio Approves. |
| Closed-catalog shortcut | High | Mitigated in tests | TEST-0018 proves registry openness; watch future hard-coded enums. |
| Cucumber-by-mistake | Med | Mitigated | TEST-0020 asserts no cucumber/behave; no `*.feature`. |
| Q-GATE-1 dual-source conflict | High | Mitigated in code | Reader returns ERROR when manifest ≠ markdown; fail closed. |
| Gatekeeper false-allow | High | Mitigated for known paths | Missing/Draft/Unknown/ERROR → deny; fixtures under `tests/fixtures/sdds/`. |
| DES-0003 scope creep into core / React | High | **Open watch** | Draft explicitly forbids impl; no `adapters/web` or xyflow on disk — keep it that way until Approved build SDD. |
| Mac ↔ box sync drift | Med | Open process | Pulse writes Mac SoT then CopyToBox → `/workspace/hextory`. |
| No `.git` / CONTRIBUTING (deferred) | High for ≥90 | Open | Blocks aspects 7 and 10 ceiling. |
| Testing-layer TBD confusion | Low | Mostly closed | DES-0002-E layout in use; residual “may change” text only. |
| Optional langgraph extra in pyproject | Low | Accepted | Optional extra only; core runtime is pure registry (AC-11 / TEST-0022). |

---

## 5. Learning for next run

- First-slice landing is the evidence bar for architecture/testability lifts — keep requiring paths + pytest counts; do not re-score as if unstarted.
- Do **not** inflate Vision/Coverage from DES-0003 Draft ideation; Studio stays non-authorizing until dual Approval of a build plan.
- Hygiene still open: scrub Approved DES-0002 leftover Draft / “may change in review”; consider Status-cell parsing that tolerates parentheticals **or** keep cells bare (`Approved` / `Draft` only).
- Next maturity ceiling is **git + CONTRIBUTING + CI gate**, not more core features (unless a new Approved SDD says otherwise).
- Watch DES-0003 scope creep (React/xyflow/`adapters/web`) and closed-catalog regressions every pulse.
- Sync Mac SoT → box after every pulse write.
- If overall < 90: continue weekday hourly pulses; if ≥ 90: pause until human restart.

---

## 6. Next actions (ordered)

1. Add `CONTRIBUTING.md` (merge-via-doc-readiness) and init/connect public git when Marcos lifts the deferral (aspects 7, 10).
2. Scrub leftover Draft / “may change in review” wording in Approved DES-0002; keep Status table cells bare for Q-GATE-1 parsing.
3. Add CI (once git exists): pytest + Approved-SDD citation check + optional import-linter (aspects 5–7).
4. Persist minimal traveler/quality FAIL report artifact beyond in-memory `routing_history` (aspect 8).
5. Keep DES-0003 as Draft ideation only — refine open questions; **do not** implement React/xyflow until a later Approved build slice.
6. Optionally list DES-0003 as Draft in `config/sdd_status.json` for manifest completeness (still deny runs).

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0006-20260916-0823.md` | This report |
| `docs/maturity/SCORECARD.md` | Rescored to R-0006 overall **53.0** |
| `docs/maturity/reports/_NEXT_FOCUS.md` | Refreshed post–first-slice |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-16 08:23 America/Santiago | Clark Bot | Initial report for R-0006 |
