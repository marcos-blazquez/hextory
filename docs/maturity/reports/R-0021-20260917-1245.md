# Hextory maturity pulse — R-0021

| Field | Value |
|---|---|
| **Report ID** | R-0021 |
| **Date / time** | 2026-09-17 12:45 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse; automation) |
| **Repo path** | Public https://github.com/marcos-blazquez/hextory @ `e5dd7d1`; box `/workspace/hextory-main-pulse`; Mac SoT connected but **stale product tree** (no `.git`, adapters=`local` only — maturity trio written here; product evidence from GitHub + box) |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0020](R-0020-20260917-1147.md) overall **74.2** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **76.4** |
| **Band** | Operational dark factory (candidate) (70–89) |
| **Meets ≥90?** | **NO** |
| **One-line summary** | Lift after PR **#15** merged dual-Approved **DES-0005** (AWS) + **DES-0006** (observability) onto main @ `e5dd7d1` (docs-only). Aspects 1–3 up; aspects **8** and **9** stay **52** / **70** — no `adapters/aws`, no MetricsPort/Prometheus exporter impl. Product code HEAD still `fcbe5b0`; tip CI **#34** Success on `e5dd7d1` (docs); product CI still **#21** on `fcbe5b0`. Pytest **84 passed / 1 skipped**. |

**Biggest blocker to 90+:** Observability still artifact-only (aspect 8 = 52) despite Approved DES-0006 — need MetricsPort + Prometheus `/metrics` + gate-denial counters; plus AWS third deploy still missing (aspect 9 = 70) despite Approved DES-0005; thin Approved *workflow* SDDs; no issue/PR templates; `required_approving_review_count: 0`.

Δ vs R-0020: **+2.2** (74.2 → 76.4). Correct lift from mainline Approved design docs only — do **not** invent aspect-8/9 lifts without implementation evidence.

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0020 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **76** | +4 | DES-0001/0002/0004/0005/0006 **Approved**; AWS third-target + factory observability direction explicit on main; DES-0003 bare **Draft** | Outsider dry-run / Approved workflow vision (~+14) |
| 2 | Design-doc coverage | **76** | +8 | TEMPLATE + five Approved SDDs (`0001`–`0002`, `0004`–`0006`); DES-0003 Draft; **still no Approved workflow SDDs** | More Approved *workflow* SDDs (~+14) |
| 3 | Review rigor (dual human+agent) | **70** | +10 | **Five** dual Approvals (`docs/design/reviews/` DES-0001/0002/0004/0005/0006); PR #15 merge-via-doc; `required_approving_review_count: 0` still caps | More duals + re-enable ≥1 approval (~+20) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **80** | 0 | DES-0004 + live `adapters/onprem` + TEST-ONP; traveler@0.1; `config/sdd_status.json` lists DES-0005/0006 Approved; **no** AWS/OBS IMPL↔TEST yet; pytest **84 passed / 1 skipped** | OBS/AWS IMPL + REQ↔TEST (~+10) |
| 5 | Architecture purity (hexagonal + open graphs) | **85** | 0 | Pure `src/` + ports; `adapters/local` + `adapters/onprem` only; open GraphRegistry; **no** `adapters/aws`; design-gates OK (6 design docs) | AWS adapter without core leak (~+5) |
| 6 | Determinism, testability & readable core | **86** | 0 | `tests/{unit,behavior,adapters}`; GWT BDD-style not Cucumber; CI bans cucumber; **84 passed / 1 skipped** reconfirmed | OBS/AWS test coverage (~+4) |
| 7 | Automation of design gates (CI/process) | **85** | 0 | Gatekeeper + Q-GATE-1 + `ci_design_gates.py` OK (6 docs); ruleset required checks; **ci #34** Success on `e5dd7d1` ([run 35241283303](https://github.com/marcos-blazquez/hextory/actions/runs/35241283303)); latest product **ci #21** on `fcbe5b0`; approvals=0 / not strict | Re-enable ≥1 approval + strict up-to-date (~+5) |
| 8 | Factory observability (traces, quality loops) | **52** | 0 | FileCheckpointer / FileIdempotency; Postgres checkpointer; DES-0006 **Approved** authorizes MetricsPort + Prometheus — **tree scan: no MetricsPort / `/metrics` exporter / Grafana / gate-denial counters** | First-slice OBS impl per DES-0006 (~+38) |
| 9 | Multi-target deploy readiness | **70** | 0 | `adapters/local` + `adapters/onprem` only; DES-0005 **Approved** authorizes AWS LocalStack/moto — **no** `adapters/aws` dir on main | AWS first slice + parity (~+20) |
| 10 | Public project readiness | **84** | 0 | Public repo + MIT + README + CONTRIBUTING; **v0.1.0**; ruleset; traveler@0.1; PRs through #15; **no** issue/PR templates (`.github/` = `workflows/ci.yml` only) | Templates + CoC/security + required approvals (~+6) |

**Readable code bar (cross-cutting):** No new core/adapter code this hour; prior walk-through-friendly modules unchanged. DES-0005/0006 are docs-only.

**Sum / overall:** 76+76+70+80+85+86+85+52+70+84 = **764** ÷ 10 = **76.4**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Strong (stable)** | Dual Approve + PR #15 docs merge; not the sole product goal. Ceilings remain observability + AWS impl. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Strong (first-slice)** | `GraphRegistry` + LangGraph-from-definition; no closed-catalog regression; DES-0005/0006 do not introduce fixed catalogs. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Strong / location in use** | `tests/{unit,behavior,adapters}`; ordinary GWT; cucumber banned; 84 green / 1 skipped. Location formally TBD but in use. |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Policy + runtime + local CI + hosted design-gates (required on main).** DES-0005/0006 **Approved** → AWS + OBS first slices **authorized**; DES-0003 Draft → no UI. |
| Human merge approval = design-doc readiness (not code walkthrough) | **Documented + PR habit** (PR #15); ruleset requires PR + status checks; **required_approving_review_count: 0**. |
| Any merge/implementation without Approved SDD? | Engine under DES-0002; on-prem under DES-0004. Product HEAD content still `fcbe5b0`. DES-0005/0006 merged as **docs-only** (commit message explicit). This pulse: maturity docs only (R-0021 artifacts). |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Crediting aspect 8/9 lifts from Approved DES-0005/0006 without impl | High | **Mitigated** | Aspects 8=52 / 9=70 held; docs themselves state those ceilings until MetricsPort/`adapters/aws` land. |
| Confusing tip CI #34 (docs Approval) with product CI | High | **Mitigated** | Cite both: tip ci #34 on `e5dd7d1` (docs) **and** product ci #21 on `fcbe5b0`. |
| Treating Authorization = Implementation complete | High | **Open** | First-slice OBS + AWS are **authorized** but not started on main; next product PRs must follow DES-0005/0006. |
| Inventing Mac SoT product progress from stale tree | High | **Mitigated** | Mac has no `.git`, no onprem/aws; evidence from GitHub `e5dd7d1` + box clone; maturity trio synced to Mac. |
| prometheus_client present in venv without MetricsPort code | Med | **Open** | Env may have dep; tree has **no** MetricsPort/`/metrics` impl — do not score OBS up. |
| Thin Approved *workflow* SDD coverage after adapter/obs Approvals | Med | **Open** | Five Approved platform SDDs; DES-0003 still Draft; no workflow SDDs dual-reviewed. |
| Closed hard-coded graph catalog regression | Med | **Mitigated** | Open registry unchanged. |
| Cucumber-only / wrong testing-layer assumption | Low | **Mitigated** | Ordinary GWT; cucumber banned; 84/1. |
| Treating code-review avoidance as sole product purpose | Med | **Mitigated** | Next actions prioritize OBS + AWS impl, not more ruleset alone. |
| Naming out-of-tree consumers in public maturity artifacts | High | **Mitigated** | Public kernel stays agnostic; this report names only public hextory paths. |

---

## 5. Learning for next run

- DES-0005/0006 **Approved on main** (`e5dd7d1` / PR #15) lifts aspects **1–3**; do **not** lift aspects **8–9** until MetricsPort/`/metrics` and `adapters/aws` (LocalStack/moto) land.
- Always separate **tip CI** (may be docs, e.g. #34 on `e5dd7d1`) from **latest product CI** (`fcbe5b0` / #21).
- Aspect 8 remains the clearest hard ceiling until DES-0006 first slice ships; HTTP 403 ≠ metrics; checkpointers ≠ dashboards.
- Aspect 9 next gap is AWS first slice under DES-0005 — LocalStack/moto, no real account deploy.
- Mac SoT may stay **connected yet stale** — prefer GitHub `main` + box `/workspace/hextory-main-pulse` for product evidence; write maturity trio to Mac; never invent Mac product progress.
- Re-run `python scripts/ci_design_gates.py` + pytest every pulse; expect ~84 passed / 1 skipped until OBS/AWS tests land.
- Public kernel stays agnostic of out-of-tree consumers; DES-0003 Draft ideation only.
- If overall < 90: continue pulses; if ≥ 90: pause until human restart.
- Do not implement product features or draft new SDDs during pulse (impl is authorized outside pulse by DES-0005/0006).

---

## 6. Next actions (ordered)

1. Observability first slice per DES-0006: MetricsPort + Prometheus `/metrics` on on-prem + local dump + TEST-OBS-* (aspect 8).
2. AWS first slice per DES-0005: `adapters/aws` LocalStack/moto + DynamoDB checkpointer + parity tests; no real AWS account (aspect 9).
3. Issue/PR templates for SDD contribution path (aspect 10).
4. More Approved *workflow* SDDs under dual review (aspects 2, 3).
5. When a second reviewer exists: re-enable ≥1 required approving review; consider strict up-to-date (aspects 3, 7).
6. Keep DES-0003 Draft ideation only; public kernel agnostic of out-of-tree consumers.
7. Continue pulses until ≥90; do not invent score lifts without new product evidence.
8. Consider refreshing Mac SoT working tree from GitHub `main` (still missing `.git` / onprem / DES-0004+).

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0021-20260917-1245.md` | This report |
| `docs/maturity/SCORECARD.md` | Rescored to R-0021 overall **76.4** (Δ+2.2 vs R-0020) |
| `docs/maturity/reports/_NEXT_FOCUS.md` | Sticky updated to R-0021 |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-17 12:45 America/Santiago | Clark Bot | R-0021 — lift to **76.4** after DES-0005/0006 dual Approve on main; OBS/AWS impl still absent |
