# Hextory maturity pulse — R-0010

| Field | Value |
|---|---|
| **Report ID** | R-0010 |
| **Date / time** | 2026-09-16 16:26 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse; manual/immediate heartbeat) |
| **Repo path** | SoT prefer Mac `/Users/mblazquez/Documents/dev/pixpod/hextory` (**unreachable this run**); box mirror `/workspace/hextory`; public https://github.com/marcos-blazquez/hextory |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0009](R-0009-20260916-1145.md) overall **59.4** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **64.8** |
| **Band** | Building foundations (40–69) — public git + live CI + interceptors/LLM |
| **Meets ≥90?** | **NO** |
| **One-line summary** | Afternoon scheduled pulses missed; this manual R-0010 **succeeds**. Verified since R-0009: **IdempotencyStore** + **QuotaTimeoutInterceptor**, **Null/Echo/Scripted LLM** + optional openai **fail-closed**, pytest **72 passed** on box, public GitHub repo live, Actions **ci #1 Success** on `03b2ebf` (badge `ci - passing`). Git-init deferral **lifted** for public Hextory. Mac SoT unreachable; box mirror updated. |

**Biggest blocker to 90+:** Branch protection / required design-gate checks on `main` still absent — hosted CI is green but not merge-enforcing. Controllable next: scrub residual Draft wording in Approved DES-0002; issue/PR templates; second deploy adapter; dashboards; more Approved workflow SDDs.

Δ vs R-0009: **+5.4** (59.4 → 64.8).

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0009 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **65** | 0 | DES-0001/0002 **Approved**; public README dark-factory thesis; DES-0003 bare **Draft**; residual “Marcos may change in review” / Draft-era wording still in Approved DES-0002-E/G | Scrub Approved DES-0002 hygiene; outsider dry-run (~+25) |
| 2 | Design-doc coverage | **60** | 0 | TEMPLATE + Approved vision + Approved engine + Draft Studio; no new Approved workflow SDDs | More Approved workflow SDDs (~+30) |
| 3 | Review rigor (dual human+agent) | **50** | 0 | Two dual Approvals + `docs/design/reviews/`; DES-0003 reviewers TBD | Dual review on more SDDs (~+40) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **74** | +6 | DES-0002 §12 + live IMPL; new TEST coverage for idempotency/quota (`tests/unit/test_idempotency_and_quota.py`) + LLM (`tests/unit/test_llm_port.py`, `tests/adapters/test_echo_llm.py`, `test_file_idempotency.py`); pytest **72 passed** | PR/commit DES citations as habit; fuller REQ↔TEST matrix (~+16) |
| 5 | Architecture purity (hexagonal + open graphs) | **80** | +4 | Pure `src/` + ports: `IdempotencyStore`, `LlmPort`/`NullLlm`/`ScriptedLlm`; adapters: `FileIdempotencyStore`, `EchoLlm`/`OpenAiCompatibleLlm` fail-closed; `langgraph` still **absent** from `src/`; `GraphRegistry` open; only `adapters/local` | Live forge habits + more workflows (~+10) |
| 6 | Determinism, testability & readable core | **84** | +6 | `tests/{unit,behavior,adapters}`; GWT BDD-style not Cucumber; CI bans cucumber; **72 passed** (langgraph present this host → no skips); ScriptedLlm + injectable clock on QuotaTimeout | Branch-required CI; scrub DES-0002-E residual (~+6) |
| 7 | Automation of design gates (CI/process) | **70** | +20 | Runtime Gatekeeper + Q-GATE-1 + `scripts/ci_design_gates.py` **OK** (3 docs, 4 keys) + `.github/workflows/ci.yml` on **main**; Actions run [35129715359](https://github.com/marcos-blazquez/hextory/actions/runs/35129715359) **Success** (design-gates + pytest matrix 3.11/3.12, 19s); **no branch protection** verified | Require design-gates (+pytest) on `main`; merge-via-doc practice (~+20) |
| 8 | Factory observability (traces, quality loops) | **52** | 0 | FileCheckpointer still: `.hextory/travelers` + quality FAIL artifacts; FileIdempotency under `.hextory/idempotency/`; **no** dashboards / central metrics | Dashboards + gate-denial metrics (~+38) |
| 9 | Multi-target deploy readiness | **40** | 0 | Still `adapters/local` only; dual runtime ≠ second deploy target; no `adapters/web` | Second target + parity matrix (~+50) |
| 10 | Public project readiness | **73** | +18 | Public https://github.com/marcos-blazquez/hextory (MIT + README + CONTRIBUTING); CI workflow on main **passing**; git deferral **lifted** for public kernel; **no** issue/PR templates; CoC/security basics thin; Mac SoT `.git` not probed (path unreachable) | Templates + branch protection + contrib dry-run (~+17) |

**Readable code bar (cross-cutting):** Interceptor chain and LLM ports remain walk-through-friendly; openai path fail-closed without keys.

**Sum / overall:** 65+60+50+74+80+84+70+52+40+73 = **648** ÷ 10 = **64.8**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Stronger (docs + live CI)** | CONTRIBUTING + Gatekeeper + hosted design-gates job green; merge-via-doc still unpracticed without required checks / PR habit. Not sole product goal — interceptors/LLM advance factory ops. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Strong (first-slice)** | `GraphRegistry` + LangGraph compiles from definition; no closed-catalog regression. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Strong / location in use** | `tests/{unit,behavior,adapters}`; 72 green; residual “Marcos may change in review” still in Approved DES-0002-E. |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Policy + runtime + local CI + hosted design-gates:** Gatekeeper denies missing/Draft/Unknown; DES-0003 Draft → no UI/React/xyflow (`adapters/local` only). |
| Human merge approval = design-doc readiness (not code walkthrough) | **Documented in CONTRIBUTING**; **not enforced** by branch protection yet. |
| Any merge/implementation without Approved SDD? | Engine slice + FileCheckpointer/LangGraph/interceptors/LLM authorized by Approved DES-0002. Public tree at `03b2ebf`. Pulse did not implement product code. |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Afternoon ~14:02 / ~16:02 pulses failed → flat scorecard | High | **Mitigated this run** | Manual R-0010 completed; rescored with verified evidence. |
| Invented interceptor/LLM progress | High | **Mitigated** | On-disk: `src/ports/idempotency.py`, `src/policies/interceptors.py` (QuotaTimeoutInterceptor), `src/ports/llm.py` (Null/Scripted), `adapters/local/file_idempotency.py`, `echo_llm.py` (openai fail-closed); pytest 72. |
| Public git claimed but absent / Actions red | High | **Mitigated** | Clone + badge `ci - passing` + run 35129715359 **Success** on `03b2ebf`. |
| Scoring live CI as fully gated merges | High | **Accepted (capped)** | Aspect 7 = 70 not maxed — **no branch protection** evidenced. |
| Dual runtime as multi-target | Med | **Accepted (no lift)** | Aspect 9 stays 40. |
| Drop FileCheckpointer credit (aspect 8) | Med | **Mitigated** | Kept 52; file artifacts still present. |
| Stale Draft wording in Approved DES-0002 | Med | **Open** | DES-0002-E “Marcos may change in review”; Draft freeze language in DES-0002-G. |
| DES-0003 scope creep / private Bruce scored as public kernel | High | **Watch OK** | No `adapters/web`; Bruce out of public score except context. |
| Mac SoT unreachable → orphaned SoT | Med | **Open** | Box mirror is authoritative this hour; sync to Mac when reachable. |
| Box mirror has no `.git` while public remote exists | Low | **Accepted** | Pulse does not `git init` on box; public clone verified separately under `/tmp/hextory-probe`. |
| Node 20 deprecation annotations on Actions | Low | **Watch** | Non-blocking warnings on checkout/setup-python. |
| `gh` unauthenticated / API rate limit | Low | **Mitigated** | Used public HTML + badge SVG + WebFetch for Actions conclusion. |

---

## 5. Learning for next run

- Git-init deferral is **lifted** for public Hextory — sticky focus must stop treating “no `.git`” as the ceiling; next ceiling is **branch protection + required checks**.
- Aspect 7/10 credit requires probing Actions **conclusion** (badge/run page), not merely presence of `ci.yml`.
- Do not max aspect 7 without branch protection / required design-gates on `main`.
- Keep FileCheckpointer observability credit; dual runtime ≠ multi-target (aspect 9 flat).
- Re-run `python scripts/ci_design_gates.py` + pytest every pulse; expect ~72 when langgraph installed.
- Hygiene still open: scrub Approved DES-0002 leftover Draft / “may change in review.”
- Mac SoT may be unreachable from box — always update `/workspace/hextory` and note sync failure.
- If overall < 90: continue weekday hourly pulses; if ≥ 90: pause until human restart.
- Do not implement product features or draft new SDDs during pulse.

---

## 6. Next actions (ordered)

1. Enable branch protection on `main`: require `design-gates` (+ pytest matrix) before merge (aspects 7, 10).
2. Scrub leftover Draft / “may change in review” wording in Approved DES-0002; keep Status table cells bare.
3. Add issue/PR templates aligned with SDD contribution path (aspect 10).
4. Keep DES-0003 as Draft ideation only — **do not** implement React/xyflow until a later Approved build slice; private Bruce stays out of public kernel score.
5. Observability next: dashboards / structured gate-denial metrics beyond file artifacts (aspect 8).
6. When Mac reachable: sync `docs/maturity/reports/R-0010-*`, `SCORECARD.md`, `_NEXT_FOCUS.md` to SoT.
7. Continue weekday hourly pulses until overall ≥ 90; do not invent score lifts without new evidence.

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0010-20260916-1626.md` | This report |
| `docs/maturity/SCORECARD.md` | Rescored to R-0010 overall **64.8** (Δ+5.4) |
| `docs/maturity/reports/_NEXT_FOCUS.md` | Git deferral lifted; branch protection next |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-16 16:26 America/Santiago | Clark Bot | Initial report for R-0010 — public git + Actions Success + interceptors/LLM evidenced |
