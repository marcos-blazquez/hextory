# Hextory maturity pulse — R-0025

| Field | Value |
|---|---|
| **Report ID** | R-0025 |
| **Date / time** | 2026-09-17 15:28 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse; automation) |
| **Repo path** | Public https://github.com/marcos-blazquez/hextory @ `6f72bde` (tip = R-0024 maturity docs PR #21); product HEAD still `14c4521` (AWS) / `884f660` (OBS); box `/workspace/hextory-main-pulse` @ `6f72bde`; Mac SoT connected but **stale product tree** (no `.git`, adapters=`local` only, missing DES-0004+) — maturity trio written to Mac; product evidence from GitHub + box |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0024](R-0024-20260917-1432.md) overall **80.5** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **80.5** |
| **Band** | Operational dark factory (candidate) (70–89) |
| **Meets ≥90?** | **NO** |
| **One-line summary** | Flat reconfirm after R-0024. No new product commits since AWS PR **#18** @ `14c4521` / OBS PR **#16** @ `884f660`. Tip is maturity docs `6f72bde` (R-0024 / PR #21) with tip CI **#47** Success; latest product CI still **#41** on `14c4521`. Local design-gates OK (6 docs); pytest **112 passed / 1 skipped**; `src/` clean of prometheus/boto; adapters `{local,onprem,aws}` present; `.github/` still workflows-only (no issue/PR templates). |

**Biggest blocker to 90+:** Observability remains first-slice (aspect **8 = 74**) — MetricsPort + `/metrics` + Grafana-as-code land, but not production-grade ops (alerts, operated scrape, traveler-journey analytics). AWS third target is moto/LocalStack parity without real-account deploy (aspect **9 = 78**). Thin Approved *workflow* SDDs; no issue/PR templates; `required_approving_review_count: 0`.

Δ vs R-0024: **0.0** (80.5 → 80.5). Honest flat reconfirm — tip `6f72bde` alone is docs-only and is not a lift.

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0024 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **76** | 0 | DES-0001/0002/0004/0005/0006 **Approved** (`config/sdd_status.json`); DES-0003 bare **Draft**; no vision-doc change this hour | Outsider dry-run / Approved workflow vision (~+14) |
| 2 | Design-doc coverage | **76** | 0 | TEMPLATE + five Approved SDDs; DES-0003 Draft; **still no Approved workflow SDDs** | More Approved *workflow* SDDs (~+14) |
| 3 | Review rigor (dual human+agent) | **70** | 0 | Five dual Approvals in `docs/design/reviews/`; PR #16/#18 under Approved DES; ruleset `required_approving_review_count: 0` caps | More duals + re-enable ≥1 approval (~+20) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **86** | 0 | DES-0004/0005/0006 live IMPL + TEST-ONP/TEST-AWS/TEST-OBS; traveler@0.1; pytest **112 passed / 1 skipped** reconfirmed on box @ `6f72bde` | Broader REQ↔TEST / workflow SDD links (~+4) |
| 5 | Architecture purity (hexagonal + open graphs) | **88** | 0 | Pure `src/` + ports; `adapters/{local,onprem,aws}`; MetricsPort Protocol (no prometheus/boto in `src/` — rg clean); open GraphRegistry | Residual adapter-surface polish (~+2) |
| 6 | Determinism, testability & readable core | **88** | 0 | `tests/{unit,behavior,adapters}`; GWT BDD-style not Cucumber; CI bans cucumber/behave/pytest-bdd; OBS+AWS suites; **112 passed / 1 skipped** | Formal testing-layer SDD + fuller edge coverage (~+2) |
| 7 | Automation of design gates (CI/process) | **85** | 0 | Gatekeeper + Q-GATE-1 + `ci_design_gates.py` OK (6 docs); tip **ci #47** Success on `6f72bde` (docs); product **ci #41** on `14c4521`; ruleset still approvals=0 / not strict | Re-enable ≥1 approval + strict up-to-date (~+5) |
| 8 | Factory observability (traces, quality loops) | **74** | 0 | MetricsPort (`src/ports/metrics.py`); on-prem GET `/metrics` (`adapters/onprem/prometheus_metrics.py`); local dump; Grafana JSON; TEST-OBS; gateway SafeMetrics. **Not** 85+: no operated alerts/prod scrape; no OTel/journey store beyond checkpointer | Production ops maturity + journey analytics (~+16) |
| 9 | Multi-target deploy readiness | **78** | 0 | Three targets: local + onprem + **aws** (Lambda/DynamoDB/moto, LocalStack Compose, uneployed SAM `template.yaml`). **No real AWS account deploy** (TEST-AWS-08 / README NG1) | Real-account / operated deploy path or stronger parity (~+12) |
| 10 | Public project readiness | **84** | 0 | Public repo + MIT + README + CONTRIBUTING; **v0.1.0**; ruleset active; traveler@0.1; PRs through #21 (R-0024); **no** issue/PR templates (`.github/` = `workflows/ci.yml` only) | Templates + CoC/security + required approvals (~+6) |

**Readable code bar (cross-cutting):** No new core/adapter code this hour; MetricsPort / Prometheus / AWS handlers remain walk-through-friendly Protocols + thin adapters.

**Sum / overall:** 76+76+70+86+88+88+85+74+78+84 = **805** ÷ 10 = **80.5**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Strong (stable)** | Dual Approve + merge-via-doc habit; ceilings remain OBS ops + AWS readiness, not more ruleset alone. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Strong (first-slice)** | `GraphRegistry` + LangGraph-from-definition; behavior test open-registry GWT; no closed-catalog regression; three adapters do not introduce fixed catalogs. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Strong / location in use** | `tests/{unit,behavior,adapters}`; ordinary GWT; cucumber banned in `ci_design_gates.py`; 112 green / 1 skipped. Location formally TBD but in use. |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Policy + runtime + local CI + hosted design-gates (required on main).** DES-0005/0006 **Approved** → AWS + OBS first slices **implemented** on main; DES-0003 Draft → no UI. |
| Human merge approval = design-doc readiness (not code walkthrough) | **Documented + PR habit**; ruleset requires PR + status checks; **required_approving_review_count: 0**. |
| Any merge/implementation without Approved SDD? | Engine under DES-0002; on-prem under DES-0004; OBS under DES-0006 (PR #16); AWS under DES-0005 (PR #18). This pulse: maturity docs only (R-0025 artifacts). |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Inventing aspect 8/9 lifts above first-slice without ops/deploy evidence | High | **Mitigated** | Held 74 / 78; MetricsPort ≠ operated alerts; moto ≠ real-account deploy. |
| Confusing tip CI #47 (R-0024 docs) with product CI | High | **Mitigated** | Cite both: tip ci #47 on `6f72bde` **and** product ci #41 on `14c4521` / OBS ci #36 on `884f660`. |
| Treating first-slice OBS as production observability | High | **Open** | `/metrics` + Grafana-as-code exist; no scrape/alerts/SLO evidence. |
| Claiming multi-target production from LocalStack/moto | High | **Open** | AWS adapter + uneployed SAM; README/TEST-AWS-08 / DES-0005 NG1 forbid real-account claim. |
| Inventing Mac SoT product progress from stale tree | High | **Mitigated** | Mac: no `.git`, adapters=`local` only, missing DES-0004+; evidence from GitHub + box; maturity trio synced to Mac. |
| Thin Approved *workflow* SDD coverage | Med | **Open** | Five Approved platform SDDs; DES-0003 Draft; no workflow SDDs dual-reviewed. |
| Closed hard-coded graph catalog regression | Med | **Mitigated** | Open registry unchanged; three adapters; `tests/behavior/test_open_registry.py` GWT. |
| Cucumber-only / wrong testing-layer assumption | Low | **Mitigated** | Ordinary GWT; cucumber/behave/pytest-bdd banned; 112/1. |
| Treating code-review avoidance as sole product purpose | Med | **Mitigated** | Next actions prioritize OBS ops + AWS readiness, not ruleset alone. |
| Naming out-of-tree consumers in public maturity artifacts | High | **Mitigated** | Public kernel stays agnostic; this report names only public hextory paths. |
| Concurrent pulse collision / inventing Δ from docs-only tip | Med | **Mitigated** | Tip moved R-0023→R-0024 docs only (`6f72bde`); product SHAs unchanged; flat Δ0. |
| Stale box mirror `/workspace/hextory` missing DES-0005/0006 + `adapters/aws` | Med | **Mitigated** | Pulse clone is product SoT; trio mirrored into stale mirror for continuity — do not use mirror for product evidence. |
| Three consecutive flat hours after OBS+AWS lift (R-0023…R-0025) | Med | **Accepted / deepen** | Sticky rule holds: only ops/deploy/templates/workflow-SDD evidence moves scores; docs-only tip commits do not. |
| Ruleset vs classic branch-protection API | Low | **Accepted** | Classic `branches/main/protection` 404; active ruleset still enforces required checks + PR. |
| gh CLI unauthenticated on box this hour | Low | **Mitigated** | Public Actions API used for run list (tip #47 / product #41 / OBS #36). |

---

## 5. Learning for next run

- R-0022 credited OBS PR #16 + AWS PR #18 → **80.5**; R-0023, R-0024, and this hour (**R-0025**) are **flat reconfirms**. Do not invent further lifts without ops/deploy/templates/workflow-SDD evidence.
- Always separate **tip CI** (may be docs, e.g. #47 on `6f72bde`) from **latest product CI** (`14c4521` / #41; OBS `884f660` / #36).
- Aspect 8 ceiling until operated scrape/alerts/traveler-journey analytics beyond first-slice MetricsPort/`/metrics`.
- Aspect 9: keep NG1 clear — no real-account deploy claim from moto/LocalStack/uneployed SAM.
- Mac SoT remains **connected yet stale** — prefer GitHub `main` + box `/workspace/hextory-main-pulse` for product evidence; write maturity trio to Mac; never invent Mac product progress; consider full refresh from GitHub `main`.
- Stale box mirror `/workspace/hextory` still lacks `adapters/aws` and DES-0005/0006 — update maturity trio there only; never treat as product SoT.
- Re-run `python scripts/ci_design_gates.py` + pytest every pulse; expect ~**112 passed / 1 skipped** with `[dev,onprem,aws]` until deeper OBS/AWS tests land.
- Public kernel stays agnostic of out-of-tree consumers; DES-0003 Draft ideation only.
- If overall < 90: continue pulses; if ≥ 90: pause until human restart.
- Do not implement product features or draft new SDDs during pulse.
- **Memory deepen (<90):** three consecutive flat hours after OBS+AWS lift — next material score moves need (a) operated OBS evidence, (b) stronger AWS path without violating NG1, (c) issue/PR templates, or (d) Approved workflow SDDs under dual review. Docs-only tip commits must not move aspect scores. Prefer public Actions API if `gh` is unauthenticated on the box.

---

## 6. Next actions (ordered)

1. Deepen observability beyond first slice (operated scrape/alerts / traveler-journey visibility) — aspect 8 toward 85+.
2. Strengthen AWS multi-target path; keep NG1 (no real-account deploy claim) clear — aspect 9.
3. Add issue/PR templates for SDD contribution path (aspect 10).
4. More Approved *workflow* SDDs under dual review (aspects 2, 3).
5. When a second reviewer exists: re-enable ≥1 required approving review; consider strict up-to-date (aspects 3, 7).
6. Keep DES-0003 Draft ideation only; public kernel agnostic of out-of-tree consumers.
7. Continue pulses until ≥90; do not invent score lifts without new product evidence.
8. Consider refreshing Mac SoT working tree from GitHub `main` (still missing `.git` / onprem / aws / DES-0004+).

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0025-20260917-1528.md` | This report |
| `docs/maturity/SCORECARD.md` | Rescored (flat 80.5) |
| `docs/maturity/reports/_NEXT_FOCUS.md` | Updated for R-0025 |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-17 15:28 America/Santiago | Clark Bot | Initial report for R-0025 — flat reconfirm after R-0024; tip `6f72bde` / ci #47 |
