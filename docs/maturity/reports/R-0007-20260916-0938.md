# Hextory maturity pulse — R-0007

| Field | Value |
|---|---|
| **Report ID** | R-0007 |
| **Date / time** | 2026-09-16 09:38 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse) |
| **Repo path** | `/Users/mblazquez/Documents/dev/pixpod/hextory` |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0006](R-0006-20260916-0823.md) overall **53.0** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **53.0** |
| **Band** | Building foundations (40–69) — first-slice scaffold (unchanged) |
| **Meets ≥90?** | **NO** |
| **One-line summary** | Re-inventory confirms **no product/docs/git delta** since R-0006; first slice still on disk (`src/` + `adapters/local` + `tests/`; **pytest 31** green); DES-0003 still Draft ideation; still **no `.git` / CONTRIBUTING / CI**. Flat pulse — ritual risk flagged. |

**Biggest blocker to 90+:** Public git + CONTRIBUTING + CI design-gate automation remain absent (aspects 7, 10 ceiling). First-slice scaffold cannot alone clear ≥90.

Δ vs R-0006: **0.0** (53.0 → 53.0). No new implementation or public-readiness artifacts since 08:23 America/Santiago.

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0006 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **65** | 0 | DES-0001/0002 **Approved** frontmatter; README status = first-slice scaffold; DES-0003 **Draft ideation only**; leftover “Draft” / “may change in review” still in Approved DES-0002 body (DES-0002-E, §15) | Outsider dry-run; scrub residual Draft wording in Approved DES-0002 (~+25) |
| 2 | Design-doc coverage | **60** | 0 | TEMPLATE + Approved vision + Approved engine + Draft Studio (`docs/design/0003-workflow-studio.md`); no new Approved workflow SDDs | More Approved workflow SDDs; do not count Draft Studio as UI impl coverage (~+30) |
| 3 | Review rigor (dual human+agent) | **50** | 0 | Two dual Approvals + agent records `docs/design/reviews/DES-0001-*.md`, `DES-0002-*.md`; DES-0003 reviewers **TBD** | Dual review on more SDDs when ready (~+40) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **65** | 0 | DES-0002 §12 + live IMPL under `src/` / `adapters/local/`; tests cite TEST-0010–0023; pytest **31** green this run | Commit/PR DES citations once git exists (~+25) |
| 5 | Architecture purity (hexagonal + open graphs) | **70** | 0 | Pure `src/` + ports; only `adapters/local`; no `adapters/web`/xyflow; `GraphRegistry` open (`src/graphs/registry.py`); ~1413 LOC src+adapters | CI import-linter; more workflows than starter (~+20) |
| 6 | Determinism, testability & readable core | **72** | 0 | `tests/{unit,behavior,adapters}`; GWT BDD-style not Cucumber (no `*.feature`); walk-through-friendly ports/gate/registry; 31 tests passed | Formal CI TDD gate; scrub DES-0002-E residual (~+18) |
| 7 | Automation of design gates (CI/process) | **28** | 0 | Runtime `PolicyGatekeeper` + `LocalSddStatusReader` fail-closed; `config/sdd_status.json`; **no `.git` / `.github` / branch protection** | Public git + CI SDD-citation + human merge-via-doc (~+62) |
| 8 | Factory observability (traces, quality loops) | **38** | 0 | Append-only `routing_history`; rework/escalate tested; CLI smoke; no persistent trace store/dashboards | Persistent FAIL/traveler artifact + dashboards (~+52) |
| 9 | Multi-target deploy readiness | **40** | 0 | `adapters/local` only (CLI, MemorySaver, SddStatusReader); on-prem/AWS deferred | Second target + parity matrix (~+50) |
| 10 | Public project readiness | **42** | 0 | MIT `LICENSE`; README accurate; **no CONTRIBUTING**, **no `.git`**, no issue/PR templates | Hosting + CONTRIBUTING + templates (~+48) |

**Readable code bar (cross-cutting):** Unchanged — small ports, explicit Gatekeeper (`src/policies/gate.py`), open registry (`src/graphs/registry.py`), fail-closed reader (`adapters/local/sdd_status_reader.py`).

**Sum / overall:** 65+60+50+65+70+72+28+38+40+42 = **530** ÷ 10 = **53.0**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Partial → Stronger in practice** | Merge-via-doc-readiness documented (README, DES-0001-B, TEST-0023). First slice under Approved DES-0002 only. No public merge practice (no git). Not sole product goal — open graphs + Studio ideation also in view. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Strong (first-slice proven)** | `GraphRegistry` + TEST-0018; starter is sample not closed catalog. No closed-catalog regression this hour. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Strong / location in use** | `tests/{unit,behavior,adapters}` live; no cucumber/behave/`*.feature`. Residual “Marcos may change in review” still in Approved DES-0002-E. |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Policy + runtime:** Gatekeeper denies missing/non-Approved; DES-0003 Draft → no UI/React/xyflow code on disk. |
| Human merge approval = design-doc readiness (not code walkthrough) | **Documented**; **unpracticed** without git merges. |
| Any merge/implementation without Approved SDD? | **No** new product code this pulse; engine slice authorized by Approved DES-0002. No `.git` on Mac SoT. |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Flat pulse / ritual without disk progress | Med→High if repeated | **Open this run** | Zero files outside maturity self-updates newer than R-0006; scores held flat deliberately — do not invent lifts. |
| Stale “Draft” / “may change in review” inside Approved DES-0002 | Med | **Open** | DES-0002-E still “Marcos may change in review”; §15 “Decided in this Draft”; DigitalTraveler “Draft freeze” wording unchanged. |
| DES-0003 Status cell prose breaks markdown parser | Med | **Open / fail-closed** | Live probe: `parse_markdown_status(0003)` → **None**; reader → **UNKNOWN** (deny). Safe today; would false-deny if Approved with parenthetical prose. |
| Manifest omits DES-0003 | Low | Open | `config/sdd_status.json` = DES-0001/0002/9999 only. |
| Closed-catalog shortcut | High | Mitigated | Registry + TEST-0018; no hard-coded catalog regression. |
| Cucumber-by-mistake | Med | Mitigated | No cucumber/behave/`*.feature` found. |
| Gatekeeper false-allow | High | Mitigated | Missing/Draft/Unknown/ERROR → deny; DES-0003 UNKNOWN path confirmed. |
| DES-0003 scope creep into core / React | High | **Watch OK** | Only `adapters/local`; no `adapters/web`/xyflow on disk. |
| Mac ↔ box sync drift | Med | Process | Pulse writes Mac SoT then mirrors maturity files → `/workspace/hextory`. |
| No `.git` / CONTRIBUTING (deferred) | High for ≥90 | Open | Still blocks aspects 7 and 10. |
| Testing-layer TBD confusion | Low | Mostly closed | Layout in use; residual “may change” text only. |

---

## 5. Learning for next run

- **Flat-pulse discipline:** If inventory shows no product/docs/git delta, hold scores and deepen edge cases — never invent metric movement.
- Re-confirmed DES-0003 Status parenthetical → parser miss → UNKNOWN fail-closed; prefer bare Status cells (`Approved` / `Draft` only) before any Studio Approval.
- Hygiene still open: scrub Approved DES-0002 leftover Draft / “may change in review.”
- Ceiling to ≥90 remains **git + CONTRIBUTING + CI design gates**, not more core features without a new Approved SDD.
- Watch DES-0003 scope creep and closed-catalog regressions every pulse.
- Sync Mac SoT → box after every pulse write.
- If overall < 90: continue weekday hourly pulses; if ≥ 90: pause until human restart.

---

## 6. Next actions (ordered)

1. Add `CONTRIBUTING.md` (merge-via-doc-readiness) and init/connect public git when Marcos lifts the deferral (aspects 7, 10).
2. Scrub leftover Draft / “may change in review” wording in Approved DES-0002; keep Status table cells bare for Q-GATE-1 parsing.
3. Add CI (once git exists): pytest + Approved-SDD citation check + optional import-linter (aspects 5–7).
4. Persist minimal traveler/quality FAIL report artifact beyond in-memory `routing_history` (aspect 8).
5. Keep DES-0003 as Draft ideation only — **do not** implement React/xyflow until a later Approved build slice.
6. Optionally list DES-0003 as Draft in `config/sdd_status.json` for manifest completeness (still deny runs).

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0007-20260916-0938.md` | This report |
| `docs/maturity/SCORECARD.md` | Rescored to R-0007 overall **53.0** (Δ0) |
| `docs/maturity/reports/_NEXT_FOCUS.md` | Refreshed; flat-pulse discipline |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-16 09:38 America/Santiago | Clark Bot | Initial report for R-0007 — flat reconfirm of R-0006 baseline |
