# Hextory maturity pulse report — R-0042

| Field | Value |
|---|---|
| **Report ID** | R-0042 |
| **Date / time** | 2026-09-18 19:21 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse; automation) |
| **Repo path** | Public https://github.com/marcos-blazquez/hextory @ `b46485b` (main tip = R-0041 docs merge, PR **#41**); product landmark unchanged: `65a46c8`; box `/workspace/hextory-pulse-r42`; evidence from GitHub `main` + this clone only |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0041](R-0041-20260918-1826.md) overall **80.9** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **80.9** |
| **Band** | Operational dark factory (candidate) (70–89) |
| **Meets ≥90?** | **NO** |
| **One-line summary** | Flat reconfirm after R-0041. `git fetch origin main` confirms main at `b46485b`; every commit after product tip `65a46c8` is maturity documentation only. Local design-gates pass and pytest is **115 passed / 1 skipped**; no new operations, deploy, workflow-SDD, or contribution-template evidence. |

**Biggest blocker to 90+:** Observability remains first-slice (aspect **8 = 74**) — MetricsPort, `/metrics`, and Grafana-as-code exist, but there is no operated scrape/alert path or traveler-journey analytics; AWS-SMOKE-001 still defers metrics scrape. AWS remains a bounded DynamoDB/table + in-process handler smoke rather than a full SAM/HTTP deployment (aspect **9 = 82**). Thin Approved *workflow* SDD coverage, missing issue/PR templates, and `required_approving_review_count: 0` remain unchanged.

Δ vs R-0041: **0.0** (80.9 → 80.9). Honest flat reconfirm: documentation tip CI is not a product maturity lift. Blockers **SAME** as R-0041.

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs prior | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---:|---:|---|---|
| 1 | Vision clarity | **76** | 0 | DES-0001/0002/0004/0005/0006 **Approved** (`config/sdd_status.json`); DES-0003 remains **Draft**; no vision change since R-0041 | Outsider dry-run / Approved workflow vision (~+14) |
| 2 | Design-doc coverage | **76** | 0 | SDD TEMPLATE + five Approved SDDs; DES-0003 Draft; no Approved workflow SDDs | More Approved *workflow* SDDs (~+14) |
| 3 | Review rigor (dual human+agent) | **70** | 0 | Five dual Approvals in `docs/design/reviews/`; design-doc review habit continues; ruleset `required_approving_review_count: 0` caps | More duals + re-enable ≥1 approval (~+20) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **86** | 0 | DES-0004/0005/0006 live IMPL + TEST-ONP/TEST-AWS/TEST-OBS; traveler@0.1; AWS-SMOKE-001; pytest **115 passed / 1 skipped** | Broader REQ↔TEST / workflow SDD links (~+4) |
| 5 | Architecture purity (hexagonal + open graphs) | **88** | 0 | Pure `src/` + ports; adapters `{local,onprem,aws}`; open `GraphRegistry`; no product changes since `65a46c8` | Residual adapter-surface polish (~+2) |
| 6 | Determinism, testability & readable core | **88** | 0 | `tests/{unit,behavior,adapters}`; readable GWT BDD-style, not Cucumber; CI bans cucumber/behave/pytest-bdd; **115 passed / 1 skipped** | Formal testing-layer SDD + fuller edge coverage (~+2) |
| 7 | Automation of design gates (CI/process) | **85** | 0 | Gatekeeper + Q-GATE-1 + `scripts/ci_design_gates.py` pass (6 docs); ruleset still approvals=0 / not strict up-to-date | Re-enable ≥1 approval + strict up-to-date (~+5) |
| 8 | Factory observability (traces, quality loops) | **74** | 0 | MetricsPort; on-prem GET `/metrics`; local dump; Grafana JSON; TEST-OBS; gateway SafeMetrics. AWS-SMOKE-001 metrics scrape is **deferred**; no operated alerts/prod scrape or journey store | Production ops maturity + journey analytics (~+16) |
| 9 | Multi-target deploy readiness | **82** | 0 | Local + on-prem + AWS Lambda/DynamoDB/moto + LocalStack Compose + uneployed SAM; AWS-SMOKE-001 **SUCCEEDED** for bounded real DynamoDB/in-process handler round-trip; no full SAM/HTTP API deploy | Full SAM/HTTP operated path or stronger continuous multi-target proof (~+8) |
| 10 | Public project readiness | **84** | 0 | Public repo + MIT + README + CONTRIBUTING; v0.1.0; ruleset active; traveler@0.1; PRs through **#41**; `.github/` remains workflows-only with no issue/PR templates | Templates + CoC/security + required approvals (~+6) |

**Readable code bar (cross-cutting):** No new core or adapter code was found after product tip `65a46c8`; no basis for a code-quality lift.

**Sum / overall:** 76+76+70+86+88+88+85+74+82+84 = **809** ÷ 10 = **80.9**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Strong (stable)** | Agents implement behind Approved SDDs and humans approve merge readiness; the solo-author ruleset remains a ceiling. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Strong (first-slice)** | `GraphRegistry` + definition-driven graph execution remain present; no closed-catalog regression. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Strong / location in use** | `tests/{unit,behavior,adapters}` with ordinary Given/When/Then-style behavior tests; tooling bans Cucumber-family assumptions; 115 green / 1 skipped. Formal location decision remains TBD. |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Held** — Gatekeeper + Q-GATE-1 + CI design-gates; six design documents pass hygiene; Draft DES-0003 remains non-authorizing |
| Human merge approval = design-doc readiness (not code walkthrough) | **Partial** — dual SDD approvals are evidenced; ruleset `required_approving_review_count: 0` remains |
| Any merge/implementation without Approved SDD? | **No evidence this pulse** — main changes since `65a46c8` are maturity docs only |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Inflating overall from docs-only tip / CI success | High | **Mitigated** | Held 80.9; main `b46485b` contains R-0041 docs after product tip `65a46c8`. |
| Treating AWS-SMOKE-001 as full multi-target or observability proof | High | **Open** | Smoke is table + in-process handler round-trip; metrics scrape and full SAM/HTTP deploy are explicitly deferred/not deployed. |
| Treating `.github/` directory as contribution templates | Med | **Mitigated** | Tree contains `.github/workflows/ci.yml` only; no issue or PR templates. |
| Closed graph / code-review-only product interpretation | Med | **Mitigated** | Open `GraphRegistry` and broader factory capabilities remain explicit; no closed-catalog or single-goal scoring. |
| Missing REQ↔TEST links or unreadable core hidden by green tests | Med | **Open** | Existing traceability/testing evidence remains good but formal testing-layer SDD and broader workflow coverage are still gaps. |
| Public API/CI evidence not authenticated | Low | **Accepted** | Local gates/tests and public main history are sufficient for this flat pulse; no unverified CI lift claimed. |

---

## 5. Learning for next run

- Re-run `git fetch origin main`, `python scripts/ci_design_gates.py`, and pytest; current local result is **115 passed, 1 skipped**.
- Flat-hour discipline remains active: docs-only maturity commits, their CI, and the pulse itself do not lift scores.
- Material score movement requires operated observability (scrape/alerts/journeys), fuller AWS SAM/HTTP evidence without violating NG1, issue/PR templates, or additional Approved workflow SDDs under dual review.
- Keep AWS-SMOKE-001 bounded: it is successful DynamoDB/in-process evidence, not full deployment or metrics operations.
- Keep the public kernel agnostic and do not draft new SDDs or implement product features during a maturity pulse.
- With overall < 90, continue the scheduled weekday hourly pulse; pause only at ≥90.

---

## 6. Next actions (ordered)

1. Deepen observability beyond first slice: operated scrape, alerts/SLOs, and traveler-journey visibility — aspect 8 toward 85+.
2. Strengthen AWS beyond table + handler smoke with a full, operated SAM/HTTP path while keeping NG1 clear — aspect 9 toward 85+.
3. Add issue/PR templates for the SDD contribution path — aspect 10.
4. Add more Approved workflow SDDs under dual review — aspects 2 and 3.
5. When a second reviewer exists, re-enable ≥1 required approval and consider strict up-to-date — aspects 3 and 7.
6. Continue pulses while overall < 90; do not invent lifts without product evidence.
7. Land the R-0042 maturity trio on `main`.

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0042-20260918-1921.md` | New flat-pulse report |
| `docs/maturity/SCORECARD.md` | Rescored/repointed to R-0042; 80.9 flat |
| `docs/maturity/reports/_NEXT_FOCUS.md` | Updated sticky evidence and next actions |

---

## Evidence appendix

| Item | Result |
|---|---|
| Main tip before this pulse land | `b46485b` (R-0041 merge, PR #41) |
| Product tip | `65a46c8` (unchanged; all commits since are maturity docs) |
| `git fetch origin main` | Confirmed `origin/main = b46485b` |
| `python scripts/ci_design_gates.py` | OK — 6 design docs; manifest keys=7 |
| pytest | **115 passed, 1 skipped, 1 warning** |
| `.github/` | `workflows/ci.yml` only — no issue/PR templates |
| AWS-SMOKE-001 metrics | **deferred** |
| AWS SAM / HTTP API | **not deployed** for this evidence slice |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-18 19:21 America/Santiago | Clark Bot (maturity pulse; automation) | Initial report for R-0042 — flat reconfirm 80.9 (Δ0 vs R-0041); blockers SAME |
