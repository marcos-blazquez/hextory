# Next pulse focus (sticky)

1. DES-0001/0002/0004/0005/0006 **Approved** on main @ `e5dd7d1` (PR #15 docs-only). Product code still `fcbe5b0` (`adapters/local` + `adapters/onprem`). R-0021 **76.4** (Δ+2.2 vs R-0020). Active ruleset requires `design-gates` + pytest on PR→main (`required_approving_review_count: 0`). Do not invent aspect-8/9 lifts from Approved SDDs without impl.
2. Ceiling to ≥90 — next items: **observability first slice** per DES-0006 (MetricsPort + Prometheus `/metrics` — aspect 8 ≈ 52); **AWS first slice** per DES-0005 LocalStack/moto (aspect 9 ≈ 70); **issue/PR templates** (aspect 10); **more Approved workflow SDDs** under dual review (aspects 2–3); re-enable required approvals when possible.
3. Capability lenses: code-review avoidance as one capability; open graphs proven; testing layer `tests/{unit,behavior,adapters}` (not Cucumber). Keep local `python scripts/ci_design_gates.py` + pytest green (~84 passed / 1 skipped with `[dev,onprem,langgraph]` until OBS/AWS tests land).
4. Product track (outside pulse): OBS first, then AWS — not DES-0003 UI. Watch: DES-0003 stays **Draft**; do not score Approved-but-unimplemented as multi-target/OBS readiness; keep public kernel agnostic of out-of-tree consumers; adapters must not fork Gatekeeper/traveler core (see `docs/contracts/digital-traveler-0.1.md`).
5. Prefer https://github.com/marcos-blazquez/hextory @ main as evidence SoT when Mac tree is missing `.git` / onprem. Separate tip CI (may be docs, e.g. ci #34 on `e5dd7d1`) from latest product CI (`fcbe5b0` / ci #21).
6. Mac SoT may be **connected yet stale** — sync maturity trio when online; never invent Mac product progress from an outdated tree; consider full refresh from GitHub `main`.
7. Always report; ≥90 self-pause unchanged. Do not draft new SDDs or implement product features during pulse (DES-0005/0006 authorize impl *outside* the pulse).
