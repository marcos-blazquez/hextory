# Next pulse focus (sticky)

1. DES-0001/0002/0004/0005/0006 **Approved**. Product on main: OBS PR **#16** @ `884f660` (MetricsPort + `/metrics` + Grafana + TEST-OBS) + AWS PR **#18** @ `14c4521` (Lambda+DynamoDB/moto, uneployed SAM). Tip maturity docs `62318d7` (R-0025 / PR #22). R-0026 **80.5** (Δ0 vs R-0025). Tip CI **#49** Success on `62318d7`; product CI **#41** on `14c4521`. Aspect **8=74**, **9=78** — do not invent further lifts without ops/deploy evidence.
2. Ceiling to ≥90 — next items: **deepen OBS ops** (alerts/operated dashboards/traveler journeys — aspect 8 toward 85+); **strengthen AWS path** without claiming real-account deploy (aspect 9); **issue/PR templates** (aspect 10); **more Approved workflow SDDs** under dual review (aspects 2–3); re-enable required approvals when possible.
3. Capability lenses: code-review avoidance as one capability; open graphs proven; testing layer `tests/{unit,behavior,adapters}` (not Cucumber). Keep local `python scripts/ci_design_gates.py` + pytest green (~**112 passed / 1 skipped** with `[dev,onprem,aws]`).
4. Product track (outside pulse): deepen OBS ops + AWS readiness — not DES-0003 UI. Watch: DES-0003 stays **Draft**; first-slice OBS ≠ production observability; moto/LocalStack ≠ real-account multi-target; keep public kernel agnostic of out-of-tree consumers; adapters must not fork Gatekeeper/traveler core (see `docs/contracts/digital-traveler-0.1.md`).
5. Prefer https://github.com/marcos-blazquez/hextory @ main as evidence SoT when Mac tree is stale. Tip = `62318d7` (ci #49 docs); product HEAD = `14c4521` (ci #41); prior OBS product = `884f660` (ci #36).
6. Mac SoT may be **connected yet stale** — sync maturity trio when online; never invent Mac product progress from an outdated tree; consider full refresh from GitHub `main`.
7. Always report; ≥90 self-pause unchanged. Do not draft new SDDs or implement product features during pulse.
8. Memory note for next run: four consecutive flat hours (R-0023…R-0026) after OBS+AWS lift — only ops/deploy/templates/workflow-SDD evidence moves scores; docs-only tip commits do not. Prefer public Actions HTML/WebFetch if `gh` is unauthenticated or API rate-limited on the box. Stash local maturity edits before `git pull` on the pulse clone. Stale box mirror `/workspace/hextory` lacks DES-0005/0006 + `adapters/aws` — maturity trio only; not product SoT.
