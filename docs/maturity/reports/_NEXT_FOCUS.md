# Next pulse focus (sticky)

1. DES-0001/0002/0004 **Approved**; **`adapters/local` + `adapters/onprem`** on main (product @ `fcbe5b0`; maturity @ `359ce5e`). R-0018 **74.2** (Δ0 vs R-0017). Active ruleset requires `design-gates` + pytest on PR→main (`required_approving_review_count: 0` for solo merges — re-enable ≥1 when a second reviewer exists). Do not invent lifts from maturity-docs-only CI greens.
2. Ceiling to ≥90 — next items: **observability** dashboards / gate-denial metrics (aspect 8 ≈ 52 — clearest hard ceiling); **third deploy target** AWS or equivalent (aspect 9 ≈ 70 — local+on-prem done); **issue/PR templates** (aspect 10); **more Approved workflow SDDs** under dual review (aspects 2–3); re-enable required approvals when possible.
3. Capability lenses: code-review avoidance as one capability; open graphs proven; testing layer `tests/{unit,behavior,adapters}` (not Cucumber). Keep local `python scripts/ci_design_gates.py` + pytest green (~84 passed / 1 skipped with `[dev,onprem,langgraph]`).
4. Product track (outside pulse): observability first, then third deploy target — not DES-0003 UI. Watch: DES-0003 stays **Draft** ideation; do not score dual runtime as multi-target; keep public kernel agnostic of out-of-tree consumers; downstream adapters and out-of-tree apps must not fork Gatekeeper/traveler core (see `docs/contracts/digital-traveler-0.1.md`).
5. Prefer https://github.com/marcos-blazquez/hextory @ main as evidence SoT when Mac offline. Flat pulses after lifts are correct — do not invent lifts from docs-only CI greens. Two consecutive flats (R-0017/R-0018) after R-0016 is expected.
6. Always report; ≥90 self-pause unchanged. Do not draft new SDDs or implement product features during pulse.
