# Next pulse focus (sticky)

1. DES-0001/0002/0004/0005/0006 **Approved**. Product on main: OBS PR **#16** @ `884f660` + AWS PR **#18** @ `14c4521` + follow-ons PR **#28** (authorized smoke + AWS-SMOKE-001 **SUCCEEDED**) + PR **#29** (DescribeTable ensure_table) → product tip **`65a46c8`**. R-0039 landed via PR **#39** @ `b6aecfb`. R-0040 **80.9** (Δ0 vs R-0039). Tip CI **Success** on `b6aecfb` (Actions run `35386858158`); prior tip Success on `95d3c92` (run `35380840156` / **#81**); product CI **#63** on `65a46c8`; smoke docs CI **#61** on `7b98737`. Aspect **8=74**, **9=82** — do not invent further lifts without ops/fuller-deploy/templates/workflow-SDD evidence.
2. Ceiling to ≥90 — next items: **deepen OBS ops** (alerts/operated dashboards/traveler journeys — aspect 8 toward 85+; smoke metrics still deferred); **strengthen AWS path** beyond table+handler smoke / fuller SAM/HTTP while NG1 keeps CI from deploying (aspect 9 toward 85+); **issue/PR templates** (aspect 10); **more Approved workflow SDDs** under dual review (aspects 2–3); re-enable required approvals when possible.
3. Capability lenses: code-review avoidance as one capability; open graphs proven; testing layer `tests/{unit,behavior,adapters}` (not Cucumber). Keep local `python scripts/ci_design_gates.py` + pytest green (~**115 passed / 1 skipped** with `[dev,onprem,aws]`).
4. Product track (outside pulse): deepen OBS ops + fuller AWS path — not DES-0003 UI. Watch: DES-0003 stays **Draft**; first-slice OBS ≠ production observability; authorized DynamoDB smoke ≠ full SAM/HTTP multi-target; keep public kernel agnostic of out-of-tree consumers; adapters must not fork Gatekeeper/traveler core (see `docs/contracts/digital-traveler-0.1.md`).
5. Prefer https://github.com/marcos-blazquez/hextory @ main as evidence SoT when Mac is offline/stale. Tip on main = `b6aecfb` (R-0039 docs); product tip = `65a46c8` (ci #63); landmarks: first AWS `14c4521` (ci #41), OBS `884f660` (ci #36), smoke docs `7b98737` (ci #61).
6. Mac SoT may be **offline or stale** — sync maturity trio when online; never invent Mac product progress; consider full refresh from GitHub `main`. Mac `sdd_status.json` may lag — verify Approved set from pulse clone / remote.
7. Always report; ≥90 self-pause unchanged. Do not draft new SDDs or implement product features during pulse.
8. Memory note for next run: R-0030 aspect-9 lift followed by R-0031…R-0040 flat reconfirms (Δ0). Only ops/fuller-deploy/templates/workflow-SDD evidence moves scores further; do not lift aspect 8 while metrics scrape remains deferred. Prefer SSH/`mafiurs` when box `gh` is unauthenticated; public Actions HTML confirms Success when REST API is rate-limited. Stale box mirror `/workspace/hextory` is not a git checkout — do not use it as product SoT. Stale OPEN PR #26 was superseded by #27; do not score it. Autonomous `gh pr merge` may require Auto-review approval — open PR + parent/human/Cloud Agent merge is acceptable when the box `gh` is logged out.
