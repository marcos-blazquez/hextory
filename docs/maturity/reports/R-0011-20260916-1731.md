# Hextory maturity pulse — R-0011

| Field | Value |
|---|---|
| **Report ID** | R-0011 |
| **Date / time** | 2026-09-16 17:31 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse; manual/immediate after user ask) |
| **Repo path** | Public https://github.com/marcos-blazquez/hextory @ `5009d20`; box mirror may lag (`/workspace`) |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0010](R-0010-20260916-1626.md) overall **64.8** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **68.3** |
| **Band** | Building foundations (40–69) — approaching operational candidate |
| **Meets ≥90?** | **NO** |
| **One-line summary** | Verified since R-0010: main HEAD `5009d20` (PR #2 agnostic scrub); prior PR #1 `db09a57` (DES-0002 Draft-era scrub + `hextory.digital_traveler@0.1`); release **v0.1.0**; ruleset **base rules** (id 23561991) **active** on main requiring `design-gates` + `pytest (3.11)` + `pytest (3.12)` with PR required; Actions on main `5009d20` **success**; local `ci_design_gates.py` OK + pytest **72 passed**. Still only `adapters/local`; no dashboards; no issue/PR templates. |

**Biggest blocker to 90+:** Multi-target deploy still thin (aspect 9 ≈ 40 — only `adapters/local`) plus weak observability (52), thin Approved workflow SDD coverage, missing issue/PR templates, and required approving reviews still **0** (caps further automation/review lifts until a second reviewer exists).

Δ vs R-0010: **+3.5** (64.8 → 68.3).

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0010 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **70** | +5 | DES-0001/0002 **Approved**; DES-0002 Approved-body hygiene landed (PR #1); public README; published traveler contract; DES-0003 remains bare **Draft** ideation (Hextory Studio Draft OK) | Outsider dry-run / more Approved workflow vision (~+20) |
| 2 | Design-doc coverage | **60** | 0 | TEMPLATE + Approved vision + Approved engine + Draft Studio; **no new Approved workflow SDDs** | More Approved workflow SDDs (~+30) |
| 3 | Review rigor (dual human+agent) | **55** | +5 | PR merge-via-doc practiced (#1/#2); still only two dual Approvals (`docs/design/reviews/`); ruleset `required_approving_review_count: 0` caps further lift | Dual review on more SDDs + re-enable ≥1 required approval when second reviewer exists (~+35) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **76** | +2 | DES-0002 §12 + live IMPL; published [`docs/contracts/digital-traveler-0.1.md`](../../contracts/digital-traveler-0.1.md) (`hextory.digital_traveler@0.1`); pytest **72 passed** | Fuller REQ↔TEST matrix / PR DES citations habit (~+14) |
| 5 | Architecture purity (hexagonal + open graphs) | **80** | 0 | Pure `src/` + ports; LangGraph/Echo/OpenAI adapter-only; open `GraphRegistry`; still only `adapters/local` | Second deploy adapter without core fork (~+10) |
| 6 | Determinism, testability & readable core | **84** | 0 | `tests/{unit,behavior,adapters}`; GWT BDD-style not Cucumber; CI bans cucumber; **72 passed** (reconfirmed this run) | More workflow coverage / branch-strict habits (~+6) |
| 7 | Automation of design gates (CI/process) | **84** | +14 | Runtime Gatekeeper + Q-GATE-1 + `ci_design_gates.py` OK; hosted CI green; ruleset **requires** `design-gates` + pytest matrix on PR→main; not 90 because `required_approving_review_count: 0` and `strict_required_status_checks_policy: false` | Re-enable ≥1 approval + strict up-to-date when feasible (~+6) |
| 8 | Factory observability (traces, quality loops) | **52** | 0 | FileCheckpointer / FileIdempotency under `.hextory/`; **no** dashboards / central metrics | Dashboards + gate-denial metrics (~+38) |
| 9 | Multi-target deploy readiness | **40** | 0 | Still `adapters/local` only; dual runtime ≠ second deploy target | Second target + parity matrix (~+50) |
| 10 | Public project readiness | **82** | +9 | Public repo + MIT + README + CONTRIBUTING; **v0.1.0** release; ruleset active; published traveler contract; PR habit (#1/#2); Actions success on `5009d20`; **no** issue/PR templates | Templates + CoC/security depth + required approvals when possible (~+8) |

**Readable code bar (cross-cutting):** Interceptor chain and LLM ports remain walk-through-friendly; no product code changed this pulse.

**Sum / overall:** 70+60+55+76+80+84+84+52+40+82 = **683** ÷ 10 = **68.3**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Stronger (ruleset + PR habit)** | Required status checks on PR→main; merge-via-doc practiced on #1/#2; approving-review count still 0 (solo merges). Not sole product goal. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Strong (first-slice)** | `GraphRegistry` + LangGraph compiles from definition; no closed-catalog regression. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Strong / location in use** | `tests/{unit,behavior,adapters}`; 72 green reconfirmed. |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Policy + runtime + local CI + hosted design-gates (required on main):** Gatekeeper denies missing/Draft/Unknown; DES-0003 Draft → no UI/React/xyflow (`adapters/local` only). |
| Human merge approval = design-doc readiness (not code walkthrough) | **Documented in CONTRIBUTING**; PR path practiced (#1/#2); ruleset requires PR + status checks; **required_approving_review_count: 0** (solo merges; re-enable later). |
| Any merge/implementation without Approved SDD? | Engine slice authorized by Approved DES-0002. Public tree at `5009d20`. Pulse docs/maturity only — no product impl. |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Scoring ruleset as fully gated merges (approvals=0 / not strict) | High | **Accepted (capped)** | Aspect 7 = 84 not 90 — required checks present; approving reviews = 0; `strict_required_status_checks_policy: false`. |
| Invented second adapter / dashboards / templates | High | **Mitigated** | On-disk: only `adapters/local`; `.github/` has `workflows/ci.yml` only; no ISSUE_TEMPLATE / PR template. |
| Dual runtime scored as multi-target | Med | **Accepted (no lift)** | Aspect 9 stays 40. |
| Naming out-of-tree consumers in public kernel docs | High | **Mitigated** | PR #2 agnostic scrub on main `5009d20`; pulse uses “downstream / out-of-tree consumers” only. |
| Claiming DES-0002 hygiene still open after PR #1 | Med | **Mitigated** | Approved-body scrub + Last updated hygiene note evidenced; residual Draft mentions only in revision history. |
| Crediting v0.1.0 / traveler@0.1 without publish proof | Med | **Mitigated** | Release tag v0.1.0 published; `docs/contracts/digital-traveler-0.1.md` present. |
| Flat aspect 2 while traveler contract landed | Low | **Accepted** | Contract ≠ new Approved workflow SDD; coverage stays 60. |
| Node 20 deprecation annotations on Actions | Low | **Watch** | Non-blocking on prior green runs. |

---

## 5. Learning for next run

- Branch-protection gap from R-0010 is **largely closed** by active ruleset required checks — sticky focus must stop treating “no branch protection” as the top automation blocker.
- Do **not** score aspect 7 at 90 while `required_approving_review_count: 0` and strict up-to-date is false.
- Release **v0.1.0** + published `hextory.digital_traveler@0.1` are done — next public-readiness ceiling is issue/PR templates (and later CoC/security depth).
- Keep dual runtime ≠ multi-target (aspect 9 flat until a second deploy adapter).
- Re-run `python scripts/ci_design_gates.py` + pytest every pulse; expect ~72 when langgraph installed.
- Keep public kernel agnostic of out-of-tree consumers; DES-0003 stays Draft ideation (Hextory Studio Draft OK).
- If overall < 90: continue weekday hourly pulses; if ≥ 90: pause until human restart.
- Do not implement product features or draft new SDDs during pulse.

---

## 6. Next actions (ordered)

1. Add a second deploy adapter (beyond `adapters/local`) with traveler/Gatekeeper parity — do not score dual runtime as multi-target (aspect 9).
2. Observability: dashboards / structured gate-denial metrics beyond file artifacts (aspect 8).
3. Add issue/PR templates aligned with SDD contribution path (aspect 10).
4. Land more Approved workflow SDDs under dual review (aspects 2, 3).
5. When a second reviewer exists: re-enable ≥1 required approving review; consider strict up-to-date checks (aspects 3, 7).
6. Keep DES-0003 as Draft ideation only — **do not** implement product UI during pulse; out-of-tree consumers stay out of public kernel score.
7. Continue weekday hourly pulses until overall ≥ 90; do not invent score lifts without new evidence.

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0011-20260916-1731.md` | This report |
| `docs/maturity/SCORECARD.md` | Rescored to R-0011 overall **68.3** (Δ+3.5) |
| `docs/maturity/reports/_NEXT_FOCUS.md` | Ruleset+v0.1.0+traveler@0.1 done; next ceilings rewritten |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-16 17:31 America/Santiago | Clark Bot | Initial report for R-0011 — ruleset + v0.1.0 + traveler@0.1 + PR habit evidenced |
