# Hextory maturity pulse — R-0008

| Field | Value |
|---|---|
| **Report ID** | R-0008 |
| **Date / time** | 2026-09-16 10:50 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse) |
| **Repo path** | `/Users/mblazquez/Documents/dev/pixpod/hextory` |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0007](R-0007-20260916-0938.md) overall **53.0** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **56.9** |
| **Band** | Building foundations (40–69) — first-slice + local design-gate automation |
| **Meets ≥90?** | **NO** |
| **One-line summary** | Verified post–R-0007 progress: `CONTRIBUTING.md`, `.github/workflows/ci.yml`, `scripts/ci_design_gates.py` (local OK), DES-0003 Status bare **Draft** + manifest entry; first slice unchanged (pytest **31** green); `.git` still absent (Marcos deferral). |

**Biggest blocker to 90+:** Public git host still deferred — CI/Actions and merge-via-doc practice cannot run on a forge until Marcos lifts `git init`/remote deferral. Controllable next gaps: scrub residual Draft wording in Approved DES-0002; persistent traveler/FAIL artifacts (aspect 8); second deploy target (aspect 9).

Δ vs R-0007: **+3.9** (53.0 → 56.9).

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0007 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **65** | 0 | DES-0001/0002 **Approved**; README cites CONTRIBUTING + CI; DES-0003 Status now bare **Draft** (parse → Draft); leftover “may change in review” / “Decided in this Draft” still in Approved DES-0002 | Scrub residual Draft wording in Approved DES-0002; outsider dry-run (~+25) |
| 2 | Design-doc coverage | **60** | 0 | TEMPLATE + Approved vision + Approved engine + Draft Studio; no new Approved workflow SDDs | More Approved workflow SDDs; Draft Studio ≠ UI coverage (~+30) |
| 3 | Review rigor (dual human+agent) | **50** | 0 | Two dual Approvals + agent records under `docs/design/reviews/`; DES-0003 reviewers TBD | Dual review on more SDDs when ready (~+40) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **65** | 0 | DES-0002 §12 + live IMPL; TEST-0010–0023; pytest **31** green this run | Commit/PR DES citations once git exists (~+25) |
| 5 | Architecture purity (hexagonal + open graphs) | **72** | +2 | Pure `src/` + ports; only `adapters/local`; `GraphRegistry` open; `ci_design_gates.py` enforces no `adapters` imports in `src/` (live OK) | Live forge CI + more workflows than starter (~+18) |
| 6 | Determinism, testability & readable core | **74** | +2 | `tests/{unit,behavior,adapters}`; GWT not Cucumber; CI script bans cucumber/behave/pytest-bdd; 31 passed; gates script green | Hosted CI TDD gate; scrub DES-0002-E residual (~+16) |
| 7 | Automation of design gates (CI/process) | **50** | +22 | Runtime Gatekeeper + fail-closed reader; **NEW** `scripts/ci_design_gates.py` (OK: 3 docs, 4 manifest keys); **NEW** `.github/workflows/ci.yml` (pytest + design-gates); still **no `.git`** / branch protection / Actions runs | Host repo + branch protection + merge-via-doc signal (~+40) |
| 8 | Factory observability (traces, quality loops) | **38** | 0 | Append-only `routing_history`; rework/escalate tested; CLI smoke; no persistent trace store/dashboards | Persistent FAIL/traveler artifact + dashboards (~+52) |
| 9 | Multi-target deploy readiness | **40** | 0 | `adapters/local` only; on-prem/AWS deferred | Second target + parity matrix (~+50) |
| 10 | Public project readiness | **55** | +13 | MIT + README; **NEW** `CONTRIBUTING.md` (merge-via-doc, hexagonal, no Cucumber/Studio creep); CI authored; **no `.git`** (intentional deferral); no issue/PR templates | Hosting when allowed + templates + CoC/security as needed (~+35) |

**Readable code bar (cross-cutting):** Unchanged core readability; design-gates script is stdlib-only, walk-through-friendly.

**Sum / overall:** 65+60+50+65+72+74+50+38+40+55 = **569** ÷ 10 = **56.9**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Stronger (docs + local gates)** | CONTRIBUTING + README encode merge-via-doc-readiness; Gatekeeper + CI design-gates enforce Approved SDD. Still unpracticed as public merges (no git). Not sole product goal. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Strong (first-slice proven)** | `GraphRegistry` + TEST-0018; starter is sample not closed catalog. No closed-catalog regression. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Strong / location in use** | `tests/{unit,behavior,adapters}`; no cucumber/behave/`*.feature`; CI script bans those deps. Residual “Marcos may change in review” still in Approved DES-0002-E. |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Policy + runtime + local CI script:** Gatekeeper denies missing/Draft/Unknown; DES-0003 Draft → no UI/React/xyflow on disk. |
| Human merge approval = design-doc readiness (not code walkthrough) | **Documented in CONTRIBUTING**; **unpracticed** without git merges. |
| Any merge/implementation without Approved SDD? | **No** new product/engine code this pulse; engine slice authorized by Approved DES-0002. No `.git` on Mac SoT. |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Ritual-without-progress after flat R-0007 | Med | **Partially closed this hour** | Real disk progress: CONTRIBUTING + CI + gates script + DES-0003 Status/manifest hygiene (mtimes 10:47 America/Santiago). Not another flat ritual. |
| Stale “Draft” / “may change in review” inside Approved DES-0002 | Med | **Open** | DES-0002-E / §15 / glossary still carry Draft-era wording. |
| DES-0003 Status parenthetical → UNKNOWN | Med | **Mitigated** | Live: Status cell bare `**Draft**`; `parse_markdown_status` → **Draft**; Gatekeeper denies DES-0003. |
| Manifest omits DES-0003 | Low | **Mitigated** | `config/sdd_status.json` includes `"DES-0003": "Draft"`. |
| CI authored but never runs (no git) | High for ≥90 | **Open / accepted until deferral lifted** | Workflow + script green locally; Actions cannot fire without host. |
| Closed-catalog shortcut | High | Mitigated | Registry + TEST-0018; no hard-coded catalog regression. |
| Cucumber-by-mistake | Med | Mitigated | No cucumber/behave/`*.feature`; CI `check_no_cucumber_dep` OK. |
| Gatekeeper false-allow | High | Mitigated | Live: missing/empty/Draft/Unknown → deny; DES-0002 → allow. |
| DES-0003 scope creep into core / React | High | **Watch OK** | Only `adapters/local`; no `adapters/web`/xyflow. |
| Mac ↔ box sync drift | Med | Process | Key-file md5 matched Mac↔box before score; maturity writes sync back via CopyFromBox. |
| Invented score lifts | High | Mitigated | Lifts limited to aspects 5–7, 10 with verified new artifacts. |

---

## 5. Learning for next run

- Treat CONTRIBUTING + local `ci_design_gates.py` + `.github/workflows/ci.yml` as **real** aspect 7/10 progress even while git remains deferred — do not re-score them as absent.
- Re-run `python scripts/ci_design_gates.py` and pytest every pulse; fail closed if they regress.
- DES-0003 Status parenthetical risk is **mitigated** (bare Draft + manifest); keep Status cells bare forever.
- Hygiene still open: scrub Approved DES-0002 leftover Draft / “may change in review.”
- Ceiling to ≥90 still requires **hosted git + live Actions + branch protection** when Marcos allows — plus observability and multi-target.
- Do not implement product features or `git init` during pulse.
- If overall < 90: continue weekday hourly pulses; if ≥ 90: pause until human restart.

---

## 6. Next actions (ordered)

1. When Marcos lifts deferral: `git init` / connect public remote so `.github/workflows/ci.yml` can run; add branch protection tied to design-gate job (aspects 7, 10).
2. Scrub leftover Draft / “may change in review” wording in Approved DES-0002; keep Status table cells bare.
3. Persist minimal traveler/quality FAIL report artifact beyond in-memory `routing_history` (aspect 8).
4. Keep DES-0003 as Draft ideation only — **do not** implement React/xyflow until a later Approved build slice.
5. Optionally add issue/PR templates once hosted.
6. Continue weekday hourly pulses until overall ≥ 90; do not invent score lifts without new evidence.

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0008-20260916-1050.md` | This report |
| `docs/maturity/SCORECARD.md` | Rescored to R-0008 overall **56.9** (Δ+3.9) |
| `docs/maturity/reports/_NEXT_FOCUS.md` | Refreshed for post-CONTRIBUTING/CI state |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-16 10:50 America/Santiago | Clark Bot | Initial report for R-0008 — verified CONTRIBUTING/CI/gates + DES-0003 hygiene |
