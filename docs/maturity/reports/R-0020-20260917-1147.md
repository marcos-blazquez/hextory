# Hextory maturity pulse — R-0020

| Field | Value |
|---|---|
| **Report ID** | R-0020 |
| **Date / time** | 2026-09-17 11:47 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse; automation) |
| **Repo path** | Public https://github.com/marcos-blazquez/hextory @ `e9ad938`; box `/workspace/hextory-main-pulse`; Mac SoT connected but **stale tree** (no `.git`, no `adapters/onprem` / DES-0004 on disk — evidence from GitHub + box) |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0019](R-0019-20260917-1106.md) overall **74.2** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **74.2** |
| **Band** | Operational dark factory (candidate) (70–89) |
| **Meets ≥90?** | **NO** |
| **One-line summary** | Flat reconfirm after R-0019 docs landed on main (`e9ad938` / PR #13). No product/code/SDD change since DES-0004 / `adapters/onprem` @ `fcbe5b0`. Local gates OK; pytest **84 passed / 1 skipped**; Actions **ci #29** Success on `e9ad938` (docs-only); latest product CI still **ci #21** Success on `fcbe5b0`. Aspect 8 still 52; AWS third target still absent; no issue/PR templates; approvals=0. |

**Biggest blocker to 90+:** Observability ceiling (aspect 8 = 52 — traveler/checkpointer artifacts without dashboards or gate-denial metrics), plus AWS/cloud third deploy adapter still missing (aspect 9 = 70), thin Approved *workflow* SDD coverage, missing issue/PR templates, and `required_approving_review_count: 0`.

Δ vs R-0019: **0.0** (74.2 → 74.2). Correct flat pulse — HEAD advanced only for R-0019 maturity docs; no new product evidence. Do not invent lifts from ci #29 green on a docs PR. Fourth consecutive flat after R-0016 (R-0017–R-0020) is expected.

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0019 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **72** | 0 | DES-0001/0002/0004 **Approved**; DES-0003 bare **Draft**; on-prem-first direction unchanged | Outsider dry-run / more Approved workflow vision (~+18) |
| 2 | Design-doc coverage | **68** | 0 | TEMPLATE + Approved vision/engine/on-prem; DES-0003 Draft; **no new Approved workflow SDDs** since R-0019 | More Approved workflow SDDs (~+22) |
| 3 | Review rigor (dual human+agent) | **60** | 0 | Three dual Approvals (`docs/design/reviews/` DES-0001/0002/0004); no new dual reviews; approvals still 0 | Dual review on more SDDs + re-enable ≥1 approval (~+30) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **80** | 0 | DES-0004 + live `adapters/onprem` + TEST-ONP; traveler@0.1; pytest **84 passed / 1 skipped** reconfirmed | Fuller REQ↔TEST matrix / workflow coverage (~+10) |
| 5 | Architecture purity (hexagonal + open graphs) | **85** | 0 | Pure `src/` + ports; `adapters/local` + `adapters/onprem` only; open GraphRegistry; **no** `adapters/aws` | AWS adapter / import-linter depth (~+5) |
| 6 | Determinism, testability & readable core | **86** | 0 | `tests/{unit,behavior,adapters}`; GWT BDD-style not Cucumber; CI bans cucumber; **84 passed / 1 skipped** | More workflow coverage (~+4) |
| 7 | Automation of design gates (CI/process) | **85** | 0 | Gatekeeper + Q-GATE-1 + `ci_design_gates.py` OK; ruleset required checks; **ci #29** Success on `e9ad938` ([run 35232402166](https://github.com/marcos-blazquez/hextory/actions/runs/35232402166)); latest product **ci #21** on `fcbe5b0` ([run 35172457651](https://github.com/marcos-blazquez/hextory/actions/runs/35172457651)); approvals=0 / not strict | Re-enable ≥1 approval + strict up-to-date (~+5) |
| 8 | Factory observability (traces, quality loops) | **52** | 0 | FileCheckpointer / FileIdempotency; Postgres checkpointer for on-prem; tree scan: **no** dashboards / Prometheus / OTel / gate-denial *metrics* impl (design-doc mentions only) | Dashboards + gate-denial metrics (~+38) |
| 9 | Multi-target deploy readiness | **70** | 0 | `adapters/local` + `adapters/onprem` only; **no** `adapters/aws` (or cloud) third target | AWS third target + parity matrix (~+20) |
| 10 | Public project readiness | **84** | 0 | Public repo + MIT + README + CONTRIBUTING; **v0.1.0**; ruleset; traveler@0.1; PRs through #13; **no** issue/PR templates (`.github/` = `workflows/ci.yml` only) | Templates + CoC/security + required approvals (~+6) |

**Readable code bar (cross-cutting):** No new core/adapter code this hour; prior walk-through-friendly modules unchanged.

**Sum / overall:** 72+68+60+80+85+86+85+52+70+84 = **742** ÷ 10 = **74.2**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Strong (stable)** | Required checks + PR habit; not the sole product goal. Ceilings remain observability + third deploy target. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Strong (first-slice)** | `GraphRegistry` + LangGraph-from-definition; no closed-catalog regression; no new graph catalog hard-coding. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Strong / location in use** | `tests/{unit,behavior,adapters}`; ordinary GWT; cucumber banned; 84 green / 1 skipped. Location formally TBD but in use. |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Policy + runtime + local CI + hosted design-gates (required on main).** DES-0003 Draft → no UI/`adapters/web`. |
| Human merge approval = design-doc readiness (not code walkthrough) | **Documented + PR habit**; ruleset requires PR + status checks; **required_approving_review_count: 0**. |
| Any merge/implementation without Approved SDD? | Engine under DES-0002; on-prem under DES-0004. Product HEAD content still `fcbe5b0` slice; maturity docs at `e9ad938`. This pulse: docs/maturity only (R-0020 artifacts). |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Inventing score lifts from R-0019 docs-only HEAD (`e9ad938` / PR #13) / ci #29 Success | High | **Mitigated** | Flat Δ0; maturity-docs CI green does not lift scores without product evidence. |
| Confusing latest Actions Success (ci #29 docs) with product CI | High | **Mitigated** | Cite both: tip ci #29 on `e9ad938` (docs) **and** latest product ci #21 on `fcbe5b0`; do not treat docs-run Success as product progress. |
| Observability ceiling misread as “Postgres = dashboards” or “gate denial 403 = metrics” | High | **Open** | Aspect 8 stays **52**; scan finds File/Postgres checkpointers + design-doc mentions only — no dashboard/Prometheus/OTel/metrics exporters. |
| Multi-target scored high with only local+onprem | High | **Mitigated** | Aspect 9 stays **70**; no `adapters/aws` (or cloud) dir. |
| Four consecutive flats (R-0017–R-0020) mistaken for stall requiring score inflation | Med | **Mitigated** | Correct after R-0016 lift; product track must move observability/AWS, not pulse invent. |
| Design-doc / review rigor lag (few Approved *workflow* SDDs) | Med | **Open** | Still only Approved vision/engine/on-prem adapter; DES-0003 Draft; no new dual reviews this hour. |
| Mac SoT connected but stale (no `.git`, missing onprem/DES-0004; maturity disk ≤ R-0014) | Med | **Accepted** | Evidence from box clone `/workspace/hextory-main-pulse` + GitHub `origin/main` @ `e9ad938`. Sync R-0020 maturity trio to Mac; do **not** invent Mac product progress. |
| Closed hard-coded graph catalog regression | Med | **Mitigated** | Open registry unchanged; on-prem reuses GraphRunner ports. |
| Cucumber-only / wrong testing-layer assumption | Low | **Mitigated** | Ordinary GWT; cucumber banned in CI; 84 passed / 1 skipped. |
| Treating code-review avoidance as sole product purpose | Med | **Mitigated** | Lenses table + next actions prioritize observability + third target, not more ruleset alone. |
| Naming out-of-tree consumers in public maturity artifacts | High | **Mitigated** | Public kernel stays agnostic; this report names only public hextory paths. |

---

## 5. Learning for next run

- Flat after a docs-only HEAD move is correct; do not invent lifts from ci greens on maturity PRs (ci #29 on `e9ad938` is hygiene, not product).
- Always separate **tip CI** (may be docs) from **latest product CI** (`fcbe5b0` / ci #21) when reporting Actions evidence.
- Aspect 8 remains the clearest hard ceiling to ≥90 until dashboards / structured gate-denial *metrics* land (product work) — HTTP 403 denial semantics ≠ observability metrics; Postgres/File checkpointers ≠ dashboards.
- Aspect 9 next gap is AWS (or equivalent third *deploy* adapter), not dual runtime or more on-prem polish.
- Four consecutive flats (R-0017–R-0020) after R-0016’s +5.9 is expected; next lift needs product evidence, not more pulse prose.
- Mac SoT may be **connected yet stale** (no git, missing onprem) — prefer GitHub `main` + box clone for product evidence; sync maturity trio when Mac is online; never invent Mac product progress from an outdated tree.
- Re-run `python scripts/ci_design_gates.py` + pytest every pulse; expect ~84 passed / 1 skipped with `[dev,onprem,langgraph]`.
- Public kernel stays agnostic of out-of-tree consumers; DES-0003 Draft ideation only; never name private sibling products in public maturity docs.
- If overall < 90: continue pulses; if ≥ 90: pause until human restart.
- Do not implement product features or draft new SDDs during pulse.

---

## 6. Next actions (ordered)

1. Observability: dashboards / structured gate-denial metrics beyond file/Postgres artifacts (aspect 8).
2. Third deploy target (AWS or equivalent) with traveler/Gatekeeper parity — do not invent aspect-9 lifts without it (aspect 9).
3. Issue/PR templates for SDD contribution path (aspect 10).
4. More Approved workflow SDDs under dual review (aspects 2, 3).
5. When a second reviewer exists: re-enable ≥1 required approving review; consider strict up-to-date (aspects 3, 7).
6. Keep DES-0003 Draft ideation only; public kernel agnostic of out-of-tree consumers.
7. Continue pulses until ≥90; do not invent score lifts without new product evidence.
8. Consider refreshing Mac SoT working tree from GitHub `main` (currently missing onprem / DES-0004 / `.git`).

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0020-20260917-1147.md` | This report |
| `docs/maturity/SCORECARD.md` | Rescored to R-0020 overall **74.2** (Δ0 vs R-0019) |
| `docs/maturity/reports/_NEXT_FOCUS.md` | Sticky updated to R-0020 |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-17 11:47 America/Santiago | Clark Bot | R-0020 — flat reconfirm **74.2** after R-0019 docs land; Mac SoT connected but stale |
