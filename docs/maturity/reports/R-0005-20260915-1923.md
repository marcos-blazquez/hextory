# Hextory maturity pulse — R-0005

| Field | Value |
|---|---|
| **Report ID** | R-0005 |
| **Date / time** | 2026-09-15 19:23 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse) |
| **Repo path** | `/Users/mblazquez/Documents/dev/pixpod/hextory` |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0004](R-0004-20260915-1815.md) overall **35.3** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **35.3** |
| **Band** | Ideation (< 40) |
| **Meets ≥90?** | **NO** |
| **One-line summary** | No material disk change since R-0004: DES-0001 and DES-0002 remain dual-**Approved** with first-slice authorized, but still no `src/`, `adapters/`, `tests/`, `.git`, CI, or CONTRIBUTING. |

**Biggest blocker to 90+:** Start the authorized DES-0002-J first slice under TDD/BDD-style (`tests/{unit,behavior,adapters}`) — Gatekeeper + SddStatusReader (Q-GATE-1 fail-closed) — then CONTRIBUTING + git/CI so design gates become automatic. Pulse did not implement (assessment only).

Δ vs R-0004: **0.0** (35.3 → 35.3). ~68 minutes with zero product-code or public-foundation progress; scores held flat by evidence rule (do not invent lifts without paths).

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0004 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **60** | 0 | DES-0001 **Approved**; DES-0002 **Approved**; README status line accurate; layout tree still labels `0002-factory-engine.md` as **Draft** (line ~46) | External dry-run + README tree hygiene (~+30) |
| 2 | Design-doc coverage | **55** | 0 | TEMPLATE + Approved vision + Approved `docs/design/0002-factory-engine.md`; intent non-gating; no other concrete workflow SDDs | First workflow SDDs beyond engine (~+35) |
| 3 | Review rigor (dual human+agent) | **50** | 0 | Two dual Approvals with agent review records under `docs/design/reviews/`; still only two SDDs | Repeatable review on more SDDs (~+40) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **40** | 0 | DES-0002 §12 maps REQ→DES→planned TEST IDs; **no IMPL/TEST files** on disk | Live IMPL/TEST links in first slice (~+50) |
| 5 | Architecture purity (hexagonal + open graphs) | **32** | 0 | DES-0002-A/C Approved (registry, not closed catalog); **no `src/`** — purity unproven | Pure core + arch/import tests (~+58) |
| 6 | Determinism, testability & readable core | **28** | 0 | DES-0002-E Approved (`tests/{unit,behavior,adapters}`; not Cucumber); DES-0002-F readable bar; **zero tests/core** | TDD/BDD-style suite + readable `src/` (~+62) |
| 7 | Automation of design gates (CI/process) | **10** | 0 | Gatekeeper + Q-GATE-1 interim on paper; still **no `.git`**, `.github`, branch protection | CI + SDD-citation + human merge-via-doc (~+80) |
| 8 | Factory observability (traces, quality loops) | **18** | 0 | Traveler + `routing_history` + rework max 3 frozen in Approved SDD; no runtime | Live traveler traces + FAIL reports (~+72) |
| 9 | Multi-target deploy readiness | **20** | 0 | DES-0002-J authorizes `adapters/local` only; on-prem/AWS deferred; **zero adapter packages** | Local CLI+MemorySaver smoke then parity (~+70) |
| 10 | Public project readiness | **40** | 0 | MIT + README Approved labels; still **no CONTRIBUTING**, no git remote, no issue/PR templates; README tree Draft stale | Hosting + CONTRIBUTING + templates (~+50) |

**Readable code bar (cross-cutting):** Still N/A for product code (none present). DES-0002-F remains the walk-through-friendly contract for future `src/`.

**Sum / overall:** 60+55+50+40+32+28+10+18+20+40 = **353** ÷ 10 = **35.3**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Partial** | DES-0002-D / README merge-via-doc-readiness clear. Not sole purpose. Unpracticed on real implementation merges. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Partial** | DES-0002-C + AC-07 + TEST-0018 Approved; starter ≠ closed catalog. First-slice hard-code shortcut risk unchanged. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Partial / location Approved** | DES-0002-E Approved layout; Cucumber forbidden (NG3). Residual “Marcos may change in review” / “Decided in this Draft” wording still in Approved body — freeze before scaffold. **No tests in repo.** |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Enforced by policy**; DES-0002 Status **Approved**, §13 green; first slice authorized. No product code present. |
| Human merge approval = design-doc readiness (not code walkthrough) | **Documented** (README, DES-0001-B, DES-0002-D) |
| Any merge/implementation without Approved SDD? | **No** — no `src/`; no `.git` on Mac SoT this run |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Authorized first slice never started (green gate, zero code) | High | **Open / deepened** | ~1h since R-0004; inventory still NO_SRC / NO_TESTS / NO_GIT / NO_CONTRIBUTING. Maturity cannot leave ideation without this. |
| Pulse ritual without progress (score flat-line) | Med | Open | Flat 35.3 is correct under evidence rule; risk is treating hourly reports as progress. Next meaningful lift needs disk paths. |
| Stale “Draft” / “may change in review” language inside Approved DES-0002 | Med | Open | Layout tree in README still Draft; DES-0002-E still says “Marcos may change in review”; §15 still has “Decided in this Draft”. Agents may misread contract hardness. |
| Intent ≠ Approved (`factory-engine-intent.md` as gate) | High | Mitigated in docs | Header says intent historical; temptation remains if agents cite intent for scope. |
| Closed catalog if DES-0002-C weakened at scaffold | High | Open | “Just hard-code three nodes” is the likely first-slice shortcut. |
| Cucumber-by-mistake for “BDD” | Med | Open | NG3 / TEST-0020 forbid; easy to add Gherkin deps at scaffold. |
| Q-GATE-1 fail-closed gaps | High | Interim recorded | No reader impl → false-allow risk until TEST-0011/0012. |
| First-slice scope creep into on-prem/AWS | High | Open | DES-0002-J / NG1. |
| Merge without SDD citation once git exists | High | Open | No CONTRIBUTING / CI yet. |
| No `.git` on Mac SoT | Med | Open | Blocks aspect 7 / public remote. |
| Mac ↔ box sync drift | Med | Open | Pulse writes Mac SoT then syncs → `/workspace/hextory`. |
| Score inflation from Approval alone | Med | Mitigated | Code/CI aspects stay ≤32; overall still <40. |
| Implementing beyond Approved first slice | High | Open | Only `src/` + `adapters/local` + `tests/` authorized; this pulse did not implement. |
| Dual-status disagreement if both frontmatter and `config/sdd_status` appear later | Med | Open | Q-GATE-1 says both must agree when both present — no fixture yet for conflict case. |
| Outsider cannot tell Approved vs Draft from tree diagram alone | Low | Open | README status line correct; tree wrong — discoverability trap for public contributors. |

---

## 5. Learning for next run

- Reconfirm: DES-0002 Approved + §13 green is settled; **blocker remains first-slice implementation + CONTRIBUTING/git/CI**, not more drafting.
- Flat scores are a signal: without new evidence paths, do not nudge numbers; deepen edge cases instead (done this run).
- Before scoring any architecture/testability lift, require concrete `src/` and `tests/` paths (failing-first TDD preferred).
- Hygiene checklist every pulse until fixed: README tree Draft label; leftover “Draft” / “may change in review” strings in Approved DES-0002; Q-GATE-1 conflict fixture once config exists.
- Watch closed-catalog / Cucumber / scope-creep / false-allow Gatekeeper every run until first slice ships.
- Sync Mac SoT → box after every pulse write.
- If overall < 90: continue weekday hourly pulses; if ≥ 90: pause until human restart.

---

## 6. Next actions (ordered)

1. **Scaffold first slice** under DES-0002-J: readable hexagonal `src/` + `adapters/local` + TDD/BDD-style tests at `tests/{unit,behavior,adapters}` (failing tests first; no Cucumber; no on-prem/AWS).
2. Implement Gatekeeper + SddStatusReader per Q-GATE-1 interim (fail closed; frontmatter and/or `config/sdd_status`).
3. Add `CONTRIBUTING.md` encoding merge-via-doc-readiness + readable-code expectations (aspect 10).
4. Init / connect public git remote; later CI for Approved SDD citation on implementation PRs (aspect 7).
5. Fix README repository-layout tree (still says DES-0002 Draft) and scrub leftover Draft / “may change in review” wording in Approved DES-0002 body.
6. Define live DigitalTraveler trace emission + minimal quality FAIL report format (aspect 8); re-score after first red→green core tests.

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0005-20260915-1923.md` | This report |
| `docs/maturity/SCORECARD.md` | Rescored to R-0005 (flat 35.3) |
| `docs/maturity/reports/_NEXT_FOCUS.md` | Refreshed; flat-line + hygiene watches |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-15 19:23 America/Santiago | Clark Bot | Initial report for R-0005 |
