# Hextory maturity pulse — R-0016

| Field | Value |
|---|---|
| **Report ID** | R-0016 |
| **Date / time** | 2026-09-17 08:23 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse; after DES-0004 / on-prem slice land) |
| **Repo path** | Public https://github.com/marcos-blazquez/hextory @ `fcbe5b0`; Cloud Agent `/workspace` |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0015](R-0015-20260916-1945.md) overall **68.3** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **74.2** |
| **Band** | Operational dark factory (candidate) (70–89) |
| **Meets ≥90?** | **NO** |
| **One-line summary** | Material lift after DES-0004 dual Approve (PR #8) + `adapters/onprem` first slice on main `fcbe5b0` (PR #9): FastAPI + JWT + Postgres + Compose with TEST-ONP parity; Actions **ci #21** Success; local gates OK; pytest **84 passed / 1 skipped**. Aspect 8 observability still ~52 (no dashboards); AWS still deferred; no issue/PR templates; approvals=0. |

**Biggest blocker to 90+:** Observability still thin (aspect 8 = 52 — file artifacts only, no dashboards / gate-denial metrics) plus AWS third target deferred, thin Approved workflow SDD coverage beyond adapter/engine, missing issue/PR templates, and `required_approving_review_count: 0`.

Δ vs R-0015: **+5.9** (68.3 → 74.2). Lift mainly from DES-0004 / `adapters/onprem` (multi-target + related design/review/arch/test deltas).

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0015 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **72** | +2 | DES-0001/0002/0004 **Approved**; on-prem-first multi-target direction explicit in DES-0004; DES-0003 bare **Draft**; README covers local + on-prem | Outsider dry-run / more Approved workflow vision (~+18) |
| 2 | Design-doc coverage | **68** | +8 | TEMPLATE + Approved vision/engine + **Approved DES-0004** on-prem adapter; DES-0003 Draft; **no new Approved workflow SDDs** | More Approved workflow SDDs (~+22) |
| 3 | Review rigor (dual human+agent) | **60** | +5 | Three dual Approvals in `docs/design/reviews/` (DES-0001/0002/0004); PR #8/#9 merge-via-doc; approvals still 0 on ruleset | Dual review on more SDDs + re-enable ≥1 approval (~+30) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **80** | +4 | DES-0004 § trace + live `adapters/onprem` IMPL + TEST-ONP-01…07; traveler@0.1 unchanged; pytest **84 passed / 1 skipped** | Fuller REQ↔TEST matrix / workflow coverage (~+10) |
| 5 | Architecture purity (hexagonal + open graphs) | **85** | +5 | Pure `src/` + ports; second deploy adapter without core fork; open `GraphRegistry`; LangGraph/Echo/OpenAI adapter-only | AWS adapter / import-linter depth (~+5) |
| 6 | Determinism, testability & readable core | **86** | +2 | `tests/{unit,behavior,adapters}`; GWT BDD-style not Cucumber; CI bans cucumber; on-prem parity under `tests/adapters/`; **84 passed / 1 skipped** | More workflow coverage (~+4) |
| 7 | Automation of design gates (CI/process) | **85** | +1 | Gatekeeper + Q-GATE-1 + local gates OK; ruleset required checks; **ci #21** Success on `fcbe5b0` ([run 35172457651](https://github.com/marcos-blazquez/hextory/actions/runs/35172457651)); approvals=0 / not strict | Re-enable ≥1 approval + strict up-to-date (~+5) |
| 8 | Factory observability (traces, quality loops) | **52** | 0 | FileCheckpointer / FileIdempotency under `.hextory/`; Postgres checkpointer persists travelers for on-prem; **no** dashboards / central metrics | Dashboards + gate-denial metrics (~+38) |
| 9 | Multi-target deploy readiness | **70** | +30 | `adapters/local` + **`adapters/onprem`** (FastAPI, JWT, Postgres, Compose) with parity tests; AWS still deferred | AWS third target + fuller parity matrix (~+20) |
| 10 | Public project readiness | **84** | +2 | Public repo + MIT + README + CONTRIBUTING; **v0.1.0**; ruleset; traveler@0.1; PRs #1–#9; on-prem run docs; **no** issue/PR templates (`.github/` = workflows only) | Templates + CoC/security + required approvals (~+6) |

**Readable code bar (cross-cutting):** On-prem adapter modules (`app.py`, `auth.py`, wiring, postgres ports) remain walk-through-friendly; core ports unchanged.

**Sum / overall:** 72+68+60+80+85+86+85+52+70+84 = **742** ÷ 10 = **74.2**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Strong (stable)** | Required checks + PR #8/#9 practice; approvals still 0. Ceilings now observability + AWS/third target, not “more ruleset.” |
| Open-ended workflow graphs (unbounded node/edge composition) | **Strong (first-slice)** | `GraphRegistry` + LangGraph-from-definition; no closed-catalog regression. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Strong / location in use** | `tests/{unit,behavior,adapters}`; 84 green / 1 skipped reconfirmed. |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Policy + runtime + local CI + hosted design-gates (required on main).** DES-0004 **Approved** authorized on-prem slice; DES-0003 Draft → no UI/`adapters/web`. |
| Human merge approval = design-doc readiness (not code walkthrough) | **Documented + PR habit (#1–#9)**; ruleset requires PR + status checks; **required_approving_review_count: 0**. |
| Any merge/implementation without Approved SDD? | Engine under DES-0002; on-prem under DES-0004. Public tree at `fcbe5b0`. This pulse: docs/maturity only. |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Inventing score lifts without DES-0004 / on-prem disk evidence | High | **Mitigated** | Lift tied to PR #8 Approve + PR #9 `adapters/onprem` @ `fcbe5b0`; not from pulse docs alone. |
| Scoring dual runtime (pure\|langgraph) as multi-target | High | **Mitigated** | Aspect 9 lift is from second **deploy** adapter (`adapters/onprem`), not dual runtime. |
| Claiming aspect 9 ≥90 with AWS still deferred | Med | **Mitigated** | Aspect 9 = 70; AWS remains explicit non-goal / deferred third target. |
| Lifting observability because Postgres checkpointer exists | High | **Mitigated** | Aspect 8 stays **52**; persistence ≠ dashboards / gate-denial metrics. |
| Naming out-of-tree consumers in public kernel docs | High | **Mitigated** | Public kernel stays agnostic; refer only to downstream/out-of-tree consumers — never name downstream products in kernel maturity docs. |
| DES-0003 Draft mistaken for Approved → UI / web adapter | Med | **Mitigated** | DES-0003 remains bare Draft ideation; no Studio UI or `adapters/web` this pulse. |
| Confusing Actions run ordinal (ci #21) with PR number (#9) | Low | **Mitigated** | Evidence cites both: PR #9 → SHA `fcbe5b0`; ci #21 = run 35172457651 Success. |
| Closed hard-coded graph catalog regression | Med | **Mitigated** | Still open registry; on-prem reuses same GraphRunner ports. |
| Cucumber-only / wrong testing-layer assumption | Low | **Mitigated** | Ordinary GWT tests; cucumber banned in CI; 84 passed / 1 skipped. |

---

## 5. Learning for next run

- DES-0004 / `adapters/onprem` cleared the long-standing aspect-9 “local-only” ceiling; next multi-target gap is AWS (or equivalent third target), not inventing further local/on-prem micro-lifts.
- Aspect 8 remains the clearest hard ceiling to ≥90 until dashboards / structured gate-denial metrics land (product work, not pulse invent).
- Keep `required_approving_review_count: 0` as a soft cap on aspects 3/7; update aspect 3/7/10 refs to latest PR / ci / HEAD each pulse.
- Re-run `python scripts/ci_design_gates.py` + pytest every pulse; expect ~84 passed / 1 skipped with `[dev,onprem,langgraph]`.
- Public kernel stays agnostic of out-of-tree consumers; DES-0003 Draft ideation only.
- If overall < 90: continue pulses; if ≥ 90: pause until human restart.
- Do not implement product features or draft new SDDs during pulse.

---

## 6. Next actions (ordered)

1. Observability: dashboards / structured gate-denial metrics beyond file/Postgres artifacts (aspect 8).
2. Third deploy target (AWS or equivalent) with traveler/Gatekeeper parity — do not invent aspect-9 lifts without it (aspect 9).
3. Issue/PR templates for SDD contribution path (aspect 10).
4. More Approved workflow SDDs under dual review (aspects 2, 3).
5. When a second reviewer exists: re-enable ≥1 required approving review; consider strict up-to-date (aspects 3, 7).
6. Keep DES-0003 Draft ideation only; public kernel agnostic of out-of-tree consumers.
7. Continue pulses until ≥90; do not invent score lifts without new product evidence.

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0016-20260917-0823.md` | This report |
| `docs/maturity/SCORECARD.md` | Rescored to R-0016 overall **74.2** (Δ+5.9 vs R-0015) |
| `docs/maturity/reports/_NEXT_FOCUS.md` | Sticky updated to R-0016 |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-17 08:23 America/Santiago | Clark Bot | R-0016 — DES-0004 / adapters/onprem lift → overall **74.2** |
