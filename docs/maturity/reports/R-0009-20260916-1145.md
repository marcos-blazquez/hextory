# Hextory maturity pulse — R-0009

| Field | Value |
|---|---|
| **Report ID** | R-0009 |
| **Date / time** | 2026-09-16 11:45 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse) |
| **Repo path** | `/Users/mblazquez/Documents/dev/pixpod/hextory` |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0008](R-0008-20260916-1050.md) overall **56.9** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **59.4** |
| **Band** | Building foundations (40–69) — first-slice + FileCheckpointer + optional LangGraph |
| **Meets ≥90?** | **NO** |
| **One-line summary** | Post–R-0008 product progress verified: **FileCheckpointer** (durable travelers + quality FAIL under `.hextory/`), **GraphRunner** port + **PureGraphRunner** default + optional **LangGraphRunner** (adapter-only; `src/` free of langgraph), CLI `--runtime pure\|langgraph` + `status --traveler`; pytest **42 passed / 3 skipped** (langgraph extra not installed this host); CI design-gates still OK; **no `.git`** (Marcos deferral). |

**Biggest blocker to 90+:** Public git host still deferred by Marcos — hosted Actions, branch protection, and merge-via-doc practice cannot run until `git init`/remote is allowed. Controllable next gaps: harden interceptors (idempotency/quota) + LLM beyond NullLlm; scrub residual Draft wording in Approved DES-0002; second deploy target; dashboards.

Δ vs R-0008: **+2.5** (56.9 → 59.4).

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0008 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **65** | 0 | DES-0001/0002 **Approved**; README documents FileCheckpointer + dual runtime; DES-0003 bare **Draft**; leftover “Marcos may change in review” / Draft-era wording still in Approved DES-0002-E/G | Scrub residual Draft wording in Approved DES-0002; outsider dry-run (~+25) |
| 2 | Design-doc coverage | **60** | 0 | TEMPLATE + Approved vision + Approved engine + Draft Studio; no new Approved workflow SDDs | More Approved workflow SDDs (~+30) |
| 3 | Review rigor (dual human+agent) | **50** | 0 | Two dual Approvals + agent records under `docs/design/reviews/`; DES-0003 reviewers TBD | Dual review on more SDDs when ready (~+40) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **68** | +3 | DES-0002 §12 + live IMPL; FileCheckpointer + GraphRunner tests; pytest **42 passed / 3 skipped** this run (would collect ~47 with langgraph) | Commit/PR DES citations once git exists; fuller LLM/interceptor TEST IDs (~+22) |
| 5 | Architecture purity (hexagonal + open graphs) | **76** | +4 | Pure `src/` + ports incl. **GraphRunner**/Checkpointer/LlmPort; **LangGraph only** in `adapters/local/langgraph_runtime.py`; grep `langgraph` in `src/` → none; `GraphRegistry` open; FileCheckpointer adapter-side | Live forge CI + more workflows than starter (~+14) |
| 6 | Determinism, testability & readable core | **78** | +4 | `tests/{unit,behavior,adapters}`; GWT in FileCheckpointer tests; CI bans cucumber; 42 green / 3 skip; optional langgraph parity skips cleanly | Hosted CI TDD gate; scrub DES-0002-E residual; install langgraph in CI matrix (~+12) |
| 7 | Automation of design gates (CI/process) | **50** | 0 | Runtime Gatekeeper + Q-GATE-1 + `ci_design_gates.py` **OK** (3 docs, 4 manifest keys) + `.github/workflows/ci.yml`; still **no `.git`** / branch protection / Actions runs | Host repo + branch protection + merge-via-doc signal (~+40) |
| 8 | Factory observability (traces, quality loops) | **52** | +14 | **NEW** FileCheckpointer: `.hextory/travelers/*.json` + `artifacts/*/quality_fail_*.json`; CLI `status --traveler` loads from disk; append-only `routing_history` retained; **no** dashboards / central trace store | Dashboards + structured gate-denial metrics (~+38) |
| 9 | Multi-target deploy readiness | **40** | 0 | Still `adapters/local` only; dual **runtime** (pure\|langgraph) ≠ second deploy target; on-prem/AWS deferred | Second target + parity matrix (~+50) |
| 10 | Public project readiness | **55** | 0 | MIT + README + CONTRIBUTING (updated for LangGraph/hex rules); CI authored; **no `.git`**; no issue/PR templates | Hosting when allowed + templates (~+35) |

**Readable code bar (cross-cutting):** FileCheckpointer and GraphRunner port remain walk-through-friendly; LangGraph compile stays behind adapter boundary.

**Sum / overall:** 65+60+50+68+76+78+50+52+40+55 = **594** ÷ 10 = **59.4**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Stronger (docs + local gates)** | CONTRIBUTING + Gatekeeper + CI design-gates; unpracticed as public merges (no git). Not sole product goal — FileCheckpointer/LangGraph advance factory ops beyond review avoidance. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Strong (first-slice + LangGraph parity)** | `GraphRegistry` + TEST-0018; LangGraphRunner compiles **from registry definition** (not a closed catalog). No closed-catalog regression. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Strong / location in use** | `tests/{unit,behavior,adapters}`; GWT FileCheckpointer tests; no cucumber/behave/`*.feature`; CI `check_no_cucumber_dep` OK. Residual “Marcos may change in review” still in Approved DES-0002-E. |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Policy + runtime + local CI script:** Gatekeeper denies missing/Draft/Unknown; DES-0003 Draft → no UI/React/xyflow on disk (`adapters/local` only). |
| Human merge approval = design-doc readiness (not code walkthrough) | **Documented in CONTRIBUTING**; **unpracticed** without git merges. |
| Any merge/implementation without Approved SDD? | Engine slice + FileCheckpointer/LangGraph adapter authorized by Approved DES-0002. No `.git` on Mac SoT. Pulse itself did not implement product code. |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Aspect 8 still “in-memory only” after R-0008 action #3 | Med | **Mitigated this hour** | FileCheckpointer + `.gitignore` `.hextory/` + CLI status from disk evidenced (mtimes ~11:10–11:39). |
| LangGraph leaks into `src/` | High | **Mitigated** | `grep langgraph` under `src/` → none; optional import only in CLI/`langgraph_runtime.py`. |
| Closed-catalog via LangGraph StateGraph hard-wire | High | **Mitigated** | Adapter builds nodes/edges from `WorkflowDefinition` registry; pure default remains. |
| Scoring dual runtime as multi-target deploy | Med | **Accepted (no lift)** | Aspect 9 stays 40 — pure\|langgraph is engine choice, not on-prem/AWS. |
| Stale Draft wording in Approved DES-0002 | Med | **Open** | DES-0002-E still “Marcos may change in review”; DES-0002-G Draft freeze language. |
| CI authored but never runs (no git) | High for ≥90 | **Open / accepted until deferral lifted** | `ci_design_gates.py` green locally. |
| Langgraph extra missing → false red | Low | **Mitigated** | 3 skips via `importorskip`; 42 passed without extra. |
| NullLlm / interceptor stubs mistaken for complete ports | Med | **Open / watch** | Idempotency + quota_timeout still stubs; LLM is NullLlm only — next track. |
| DES-0003 scope creep | High | **Watch OK** | No `adapters/web`/xyflow. |
| Invented score lifts | High | **Mitigated** | Lifts limited to aspects 4–6, 8 with verified new files/tests. |

---

## 5. Learning for next run

- Treat FileCheckpointer + GraphRunner/LangGraph adapter as **real** aspect 5/6/8 progress — do not re-score observability as in-memory-only.
- Dual runtime ≠ multi-target: keep aspect 9 flat until a second deploy adapter exists.
- Re-run `python3 scripts/ci_design_gates.py` and pytest every pulse; note skip count when langgraph absent.
- Hygiene still open: scrub Approved DES-0002 leftover Draft / “may change in review.”
- Next product track (not this pulse): harden interceptors (idempotency/quota) then LLM beyond NullLlm.
- Ceiling to ≥90 still requires **hosted git + live Actions + branch protection** when Marcos allows.
- Do not implement product features or `git init` during pulse.
- If overall < 90: continue weekday hourly pulses; if ≥ 90: pause until human restart.

---

## 6. Next actions (ordered)

1. When Marcos lifts deferral: `git init` / connect public remote so `.github/workflows/ci.yml` can run; branch protection on design-gate job (aspects 7, 10).
2. Scrub leftover Draft / “may change in review” wording in Approved DES-0002; keep Status table cells bare.
3. Product track (outside pulse): harden idempotency + quota/timeout interceptors; then LLM port beyond NullLlm.
4. Keep DES-0003 as Draft ideation only — **do not** implement React/xyflow until a later Approved build slice.
5. Observability next: dashboards / structured gate-denial metrics beyond file artifacts (aspect 8).
6. Continue weekday hourly pulses until overall ≥ 90; do not invent score lifts without new evidence.

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0009-20260916-1145.md` | This report |
| `docs/maturity/SCORECARD.md` | Rescored to R-0009 overall **59.4** (Δ+2.5) |
| `docs/maturity/reports/_NEXT_FOCUS.md` | Refreshed for FileCheckpointer + LangGraph state |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-16 11:45 America/Santiago | Clark Bot | Initial report for R-0009 — FileCheckpointer + GraphRunner/LangGraph evidenced |
