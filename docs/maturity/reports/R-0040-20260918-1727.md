# Hextory maturity pulse — R-0040

| Field | Value |
|---|---|
| **Report ID** | R-0040 |
| **Date / time** | 2026-09-18 17:27 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse; automation) |
| **Repo path** | Public https://github.com/marcos-blazquez/hextory @ `b6aecfb` (tip = R-0039 maturity docs PR **#39** merge); product landmarks unchanged: OBS `884f660`, AWS adapter `14c4521`, smoke docs `7b98737`, DescribeTable fix `65a46c8`; box `/workspace/hextory-pulse-r40` @ `b6aecfb`; **Mac SoT not used this run** — evidence from GitHub `main` + this clone only |
| **Method** | ASPECTS.md; scores 0–100; overall = mean |
| **Template** | REPORT_TEMPLATE.md |
| **Prior pulse** | R-0039 overall **80.9** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **80.9** |
| **Band** | Operational dark factory (candidate) (70–89) |
| **Meets ≥90?** | **NO** |
| **One-line summary** | Flat reconfirm after R-0039. No product commits on `main` since product tip `65a46c8`. Tip `b6aecfb` is the R-0039 docs land (PR **#39** merge). Tip CI **Success** on `b6aecfb` (Actions run `35386858158`). Local design-gates OK (6 docs); pytest **115 passed / 1 skipped**; AWS-SMOKE metrics scrape still **deferred**; `.github/` still workflows-only (no issue/PR templates). |

**Biggest blocker to 90+:** Observability remains first-slice (aspect **8 = 74**) — MetricsPort + `/metrics` + Grafana-as-code land, but not production-grade ops (alerts, operated scrape, traveler-journey analytics); AWS-SMOKE metrics scrape **deferred**. AWS third target has bounded real-account DynamoDB smoke (aspect **9 = 82**) but **no full SAM/HTTP API deploy**. Thin Approved *workflow* SDDs; no issue/PR templates; `required_approving_review_count: 0`.

Δ vs R-0039: **0.0** (80.9 → 80.9). Honest flat reconfirm — maturity tip / CI on docs alone are not a lift. Tenth consecutive flat pulse after R-0030's aspect-9 move. No invented lifts. Blockers **SAME** as R-0039.

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0039 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **76** | 0 | DES-0001/0002/0004/0005/0006 **Approved** (`config/sdd_status.json`); DES-0003 bare **Draft**; no vision-doc change since R-0039 | Outsider dry-run / Approved workflow vision (~+14) |
| 2 | Design-doc coverage | **76** | 0 | TEMPLATE + five Approved SDDs; DES-0003 Draft; **still no Approved workflow SDDs** | More Approved *workflow* SDDs (~+14) |
| 3 | Review rigor (dual human+agent) | **70** | 0 | Five dual Approvals in `docs/design/reviews/`; PR #16/#18/#28/#29/#30–#39 under Approved DES / docs path; ruleset `required_approving_review_count: 0` caps | More duals + re-enable ≥1 approval (~+20) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **86** | 0 | DES-0004/0005/0006 live IMPL + TEST-ONP/TEST-AWS/TEST-OBS; traveler@0.1; evidence note `docs/maturity/evidence/AWS-SMOKE-001.md`; pytest **115 passed / 1 skipped** on pulse clone @ `b6aecfb` | Broader REQ↔TEST / workflow SDD links (~+4) |
| 5 | Architecture purity (hexagonal + open graphs) | **88** | 0 | Pure `src/` + ports; `adapters/{local,onprem,aws}`; open GraphRegistry. No new adapter code since `65a46c8` | Residual adapter-surface polish (~+2) |
| 6 | Determinism, testability & readable core | **88** | 0 | `tests/{unit,behavior,adapters}`; GWT BDD-style not Cucumber; CI bans cucumber/behave/pytest-bdd; OBS+AWS suites; **115 passed / 1 skipped** | Formal testing-layer SDD + fuller edge coverage (~+2) |
| 7 | Automation of design gates (CI/process) | **85** | 0 | Gatekeeper + Q-GATE-1 + `ci_design_gates.py` OK (6 docs); tip CI **Success** on `b6aecfb` (Actions run `35386858158`); prior tip Success on `95d3c92` (run `35380840156` / **#81**); product **#63** on `65a46c8`; ruleset still approvals=0 / not strict up-to-date | Re-enable ≥1 approval + strict up-to-date (~+5) |
| 8 | Factory observability (traces, quality loops) | **74** | 0 | MetricsPort (`src/ports/metrics.py`); on-prem GET `/metrics`; local dump; Grafana JSON; TEST-OBS; gateway SafeMetrics. AWS-SMOKE-001 explicitly **defers metrics scrape**. **Not** 85+: no operated alerts/prod scrape; no OTel/journey store beyond checkpointer. No new OBS commit this pulse | Production ops maturity + journey analytics (~+16) |
| 9 | Multi-target deploy readiness | **82** | 0 | Three targets: local + onprem + **aws** (Lambda/DynamoDB/moto, LocalStack Compose, undeployed SAM). Prior: authorized AWS-SMOKE-001 **SUCCEEDED** + DescribeTable `ensure_table` (PR #28/#29). Still **no full SAM/HTTP API deploy**; CI remains moto-only (NG1). No new multi-target evidence; no real AWS deploy this run | Full SAM/HTTP operated path or stronger continuous multi-target proof (~+8) |
| 10 | Public project readiness | **84** | 0 | Public repo + MIT + README + CONTRIBUTING; **v0.1.0**; ruleset active; traveler@0.1; PRs through **#39**; **no** issue/PR templates (`.github/` = `workflows/ci.yml` only). Stale PR **#26** still OPEN (superseded by merged #27) — not a readiness lift | Templates + CoC/security + required approvals (~+6) |

**Readable code bar (cross-cutting):** No new core/adapter code this pulse; product tree unchanged since `65a46c8`.

**Sum / overall:** 76+76+70+86+88+88+85+74+82+84 = **809** ÷ 10 = **80.9**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Strong (stable)** | Dual Approve + merge-via-doc habit; ceilings remain OBS ops + fuller AWS path, not more ruleset alone. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Strong (first-slice)** | `GraphRegistry` + LangGraph-from-definition; behavior test open-registry GWT; no closed-catalog regression; three adapters do not introduce fixed catalogs. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Strong / location in use** | `tests/{unit,behavior,adapters}`; ordinary GWT; cucumber banned in `ci_design_gates.py`; 115 green / 1 skipped. Location formally TBD but in use. |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Held** — Gatekeeper + Q-GATE-1 + CI design-gates; five Approved SDDs; DES-0003 Draft stays non-authorizing |
| Human merge approval = design-doc readiness (not code walkthrough) | **Partial** — dual Approvals on SDDs; PR ruleset `required_approving_review_count: 0` (solo-author path) |
| Any merge/implementation without Approved SDD? | **No** this pulse — tip is docs-only R-0039 land; product tip still under DES-0005/0006 |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Inflating overall from docs-only tip / CI Success | High | Mitigated | Δ0 held; tip `b6aecfb` is R-0039 docs only |
| Treating AWS-SMOKE-001 as full multi-target / OBS ops proof | High | Open | Smoke SUCCEEDED but metrics scrape **deferred**; no SAM/HTTP deploy |
| Stale OPEN PR #26 scored as progress | Med | Mitigated | Superseded by #27; not scored |
| Public API rate-limit hiding authenticated CI details | Low | Accepted | Cited Actions run id `35386858158` + Success via public HTML |
| Mac SoT offline/stale → scoring from laptop tree | Med | Mitigated | Prefer GitHub `main` + pulse clone; Mac not used (disconnected) |
| Inventing workflow-SDD / template lifts | High | Mitigated | Still absent; aspects 2/10 flat |

---

## 5. Learning for next run

- Re-run `python scripts/ci_design_gates.py` + pytest every pulse; expect ~**115 passed / 1 skipped** with `[dev,onprem,aws]` until deeper OBS/AWS tests land. This run: design-gates OK (6 docs); pytest **115 passed, 1 skipped**.
- Public kernel stays agnostic of out-of-tree consumers; DES-0003 Draft ideation only.
- If overall < 90: continue pulses; if ≥ 90: pause until human restart.
- Do not implement product features or draft new SDDs during pulse.
- **Memory deepen (<90):** next material moves need (a) operated OBS evidence (scrape/alerts/journeys), (b) fuller AWS path (SAM/HTTP API) without violating NG1 CI rule, (c) issue/PR templates, or (d) Approved workflow SDDs under dual review. Do not lift aspect 8 from smoke alone while metrics remain deferred. Prefer SSH/`mafiurs` when box `gh` is unauthenticated; public Actions HTML confirms Success when REST API is rate-limited. Stale box mirror `/workspace/hextory` is not a git SoT. Stale OPEN PR #26 must not be scored as new work. Flat hours remain scoring discipline when no new ops/deploy/templates/workflow-SDD evidence lands. Box `gh` logged out → push branch via SSH then Cloud Agent (or parent) for `gh pr create`/`merge`.

---

## 6. Next actions (ordered)

1. Deepen observability beyond first slice (operated scrape/alerts / traveler-journey visibility) — aspect 8 toward 85+.
2. Strengthen AWS multi-target path beyond table+handler smoke (fuller SAM/HTTP evidence) while keeping NG1 (no CI real-account deploy) clear — aspect 9 toward 85+.
3. Add issue/PR templates for SDD contribution path (aspect 10).
4. More Approved *workflow* SDDs under dual review (aspects 2, 3).
5. When a second reviewer exists: re-enable ≥1 required approving review; consider strict up-to-date (aspects 3, 7).
6. Keep DES-0003 Draft ideation only — no product UI during pulse; out-of-tree consumers stay out of public kernel score.
7. Continue weekday hourly pulses until overall ≥ 90; do not invent score lifts without new evidence.
8. Land R-0040 maturity trio on main (R-0039 already via #39).
9. When Mac returns online: refresh SoT working tree from GitHub `main`.

---

## 7. Evidence appendix (this run)

| Item | Result |
|---|---|
| Tip SHA before land | `b6aecfb` (merge PR #39 / R-0039) |
| Product tip | `65a46c8` (unchanged) |
| `python scripts/ci_design_gates.py` | OK — 6 design docs; manifest keys=7 |
| pytest `[dev,onprem,aws]` | **115 passed, 1 skipped** |
| Tip CI | **Success** — Actions run `35386858158` on `b6aecfb` |
| `.github/` | `workflows/ci.yml` only — no issue/PR templates |
| AWS-SMOKE-001 metrics | **deferred** (unchanged) |
| Mac SoT | Disconnected — not used |
| Private sibling product | Not mentioned in public docs this run |
