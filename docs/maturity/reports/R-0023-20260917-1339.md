# Hextory maturity pulse — R-0023

| Field | Value |
|---|---|
| **Report ID** | R-0023 |
| **Date / time** | 2026-09-17 13:39 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse; automation) |
| **Repo path** | Public https://github.com/marcos-blazquez/hextory @ `965c201` (tip = R-0022 maturity docs); product HEAD still `14c4521` (AWS) / `884f660` (OBS); box `/workspace/hextory-main-pulse`; Mac SoT connected but **stale product tree** (no `.git`, adapters=`local` only) — maturity trio written to Mac; product evidence from GitHub + box |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0022](R-0022-20260917-1327.md) overall **80.5** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **80.5** |
| **Band** | Operational dark factory (candidate) (70–89) |
| **Meets ≥90?** | **NO** |
| **One-line summary** | Flat reconfirm after R-0022. No new product commits since AWS PR **#18** @ `14c4521` / OBS PR **#16** @ `884f660`. Tip is maturity docs `965c201` with tip CI **#43** Success; latest product CI still **#41** on `14c4521`. Design-gates OK (6 docs); pytest **112 passed / 1 skipped**; `src/` clean of prometheus/boto; adapters `{local,onprem,aws}` present; `.github/` still workflows-only (no issue/PR templates). |

**Biggest blocker to 90+:** Observability remains first-slice (aspect **8 = 74**) — MetricsPort + `/metrics` + Grafana-as-code land, but not production-grade ops (alerts, operated scrape, traveler-journey analytics). AWS third target is moto/LocalStack parity without real-account deploy (aspect **9 = 78**). Thin Approved *workflow* SDDs; no issue/PR templates; `required_approving_review_count: 0`.

Δ vs R-0022: **0.0** (80.5 → 80.5). Honest flat reconfirm — no invent lifts without new product evidence.

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0022 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **76** | 0 | DES-0001/0002/0004/0005/0006 **Approved**; DES-0003 bare **Draft**; no vision-doc change this hour | Outsider dry-run / Approved workflow vision (~+14) |
| 2 | Design-doc coverage | **76** | 0 | TEMPLATE + five Approved SDDs; DES-0003 Draft; **still no Approved workflow SDDs** | More Approved *workflow* SDDs (~+14) |
| 3 | Review rigor (dual human+agent) | **70** | 0 | Five dual Approvals in `docs/design/reviews/`; PR #16/#18 under Approved DES; `required_approving_review_count: 0` caps | More duals + re-enable ≥1 approval (~+20) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **86** | 0 | DES-0004/0005/0006 live IMPL + TEST-ONP/TEST-AWS/TEST-OBS; traveler@0.1; `config/sdd_status.json` Approved×5; pytest **112 passed / 1 skipped** reconfirmed | Broader REQ↔TEST / workflow SDD links (~+4) |
| 5 | Architecture purity (hexagonal + open graphs) | **88** | 0 | Pure `src/` + ports; `adapters/{local,onprem,aws}`; MetricsPort Protocol (no prometheus/boto in `src/` — rg clean); open GraphRegistry | Residual adapter-surface polish (~+2) |
| 6 | Determinism, testability & readable core | **88** | 0 | `tests/{unit,behavior,adapters}`; GWT BDD-style not Cucumber; CI bans cucumber; OBS+AWS suites; **112 passed / 1 skipped** | Formal testing-layer SDD + fuller edge coverage (~+2) |
| 7 | Automation of design gates (CI/process) | **85** | 0 | Gatekeeper + Q-GATE-1 + `ci_design_gates.py` OK (6 docs); tip **ci #43** Success on `965c201` (docs); product **ci #41** on `14c4521`; ruleset still approvals=0 / not strict | Re-enable ≥1 approval + strict up-to-date (~+5) |
| 8 | Factory observability (traces, quality loops) | **74** | 0 | MetricsPort (`src/ports/metrics.py`); on-prem GET `/metrics` (`adapters/onprem/prometheus_metrics.py`); local dump; Grafana JSON; TEST-OBS; gateway SafeMetrics. **Not** 85+: no operated alerts/prod scrape; no OTel/journey store beyond checkpointer | Production ops maturity + journey analytics (~+16) |
| 9 | Multi-target deploy readiness | **78** | 0 | Three targets: local + onprem + **aws** (Lambda/DynamoDB/moto, LocalStack Compose, uneployed SAM `template.yaml`). **No real AWS account deploy** (TEST-AWS-08 / README) | Real-account / operated deploy path or stronger parity (~+12) |
| 10 | Public project readiness | **84** | 0 | Public repo + MIT + README + CONTRIBUTING; **v0.1.0**; ruleset; traveler@0.1; PRs through #18; **no** issue/PR templates (`.github/` = `workflows/ci.yml` only) | Templates + CoC/security + required approvals (~+6) |

**Readable code bar (cross-cutting):** No new core/adapter code this hour; MetricsPort / Prometheus / AWS handlers remain walk-through-friendly Protocols + thin adapters.

**Sum / overall:** 76+76+70+86+88+88+85+74+78+84 = **805** ÷ 10 = **80.5**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Strong (stable)** | Dual Approve + merge-via-doc habit; ceilings remain OBS ops + AWS readiness, not more ruleset alone. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Strong (first-slice)** | `GraphRegistry` + LangGraph-from-definition; no closed-catalog regression; three adapters do not introduce fixed catalogs. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Strong / location in use** | `tests/{unit,behavior,adapters}`; ordinary GWT; cucumber banned; 112 green / 1 skipped. Location formally TBD but in use. |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Policy + runtime + local CI + hosted design-gates (required on main).** DES-0005/0006 **Approved** → AWS + OBS first slices **implemented** on main; DES-0003 Draft → no UI. |
| Human merge approval = design-doc readiness (not code walkthrough) | **Documented + PR habit**; ruleset requires PR + status checks; **required_approving_review_count: 0**. |
| Any merge/implementation without Approved SDD? | Engine under DES-0002; on-prem under DES-0004; OBS under DES-0006 (PR #16); AWS under DES-0005 (PR #18). This pulse: maturity docs only (R-0023 artifacts). |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Inventing aspect 8/9 lifts above first-slice without ops/deploy evidence | High | **Mitigated** | Held 74 / 78; MetricsPort ≠ operated alerts; moto ≠ real-account deploy. |
| Confusing tip CI #43 (R-0022 docs) with product CI | High | **Mitigated** | Cite both: tip ci #43 on `965c201` **and** product ci #41 on `14c4521` / OBS ci #36 on `884f660`. |
| Treating first-slice OBS as production observability | High | **Open** | `/metrics` + Grafana-as-code exist; no scrape/alerts/SLO evidence. |
| Claiming multi-target production from LocalStack/moto | High | **Open** | AWS adapter + uneployed SAM; README/TEST-AWS-08 forbid real-account claim. |
| Inventing Mac SoT product progress from stale tree | High | **Mitigated** | Mac: no `.git`, adapters=`local` only, missing DES-0004+; evidence from GitHub + box; maturity trio synced to Mac. |
| Thin Approved *workflow* SDD coverage | Med | **Open** | Five Approved platform SDDs; DES-0003 Draft; no workflow SDDs dual-reviewed. |
| Closed hard-coded graph catalog regression | Med | **Mitigated** | Open registry unchanged; three adapters. |
| Cucumber-only / wrong testing-layer assumption | Low | **Mitigated** | Ordinary GWT; cucumber banned; 112/1. |
| Treating code-review avoidance as sole product purpose | Med | **Mitigated** | Next actions prioritize OBS ops + AWS readiness, not ruleset alone. |
| Naming out-of-tree consumers in public maturity artifacts | High | **Mitigated** | Public kernel stays agnostic; this report names only public hextory paths. |
| Concurrent pulse collision (R-0022 already on main at 13:27) | Med | **Mitigated** | This run is **R-0023** (13:39); flat reconfirm of R-0022 scores. |

---

## 5. Learning for next run

- R-0022 already credited OBS PR #16 + AWS PR #18 → **80.5**. This hour: **flat reconfirm**; do not invent further lifts without ops/deploy/templates/workflow-SDD evidence.
- Always separate **tip CI** (may be docs, e.g. #43 on `965c201`) from **latest product CI** (`14c4521` / #41; OBS `884f660` / #36).
- Aspect 8 ceiling until operated scrape/alerts/traveler-journey analytics beyond first-slice MetricsPort/`/metrics`.
- Aspect 9: keep NG1 clear — no real-account deploy claim from moto/LocalStack/uneployed SAM.
- Mac SoT remains **connected yet stale** — prefer GitHub `main` + box `/workspace/hextory-main-pulse` for product evidence; write maturity trio to Mac; never invent Mac product progress; consider full refresh from GitHub `main`.
- Re-run `python scripts/ci_design_gates.py` + pytest every pulse; expect ~**112 passed / 1 skipped** with `[dev,onprem,aws]` until deeper OBS/AWS tests land.
- Public kernel stays agnostic of out-of-tree consumers; DES-0003 Draft ideation only.
- If overall < 90: continue pulses; if ≥ 90: pause until human restart.
- Do not implement product features or draft new SDDs during pulse.

---

## 6. Next actions (ordered)

1. Deepen observability beyond first slice (operated scrape/alerts / traveler-journey visibility) — aspect 8 toward 85+.
2. Strengthen AWS multi-target path; keep NG1 (no real-account deploy claim) clear — aspect 9.
3. Add issue/PR templates for SDD contribution path (aspect 10).
4. More Approved *workflow* SDDs under dual review (aspects 2, 3).
5. When a second reviewer exists: re-enable ≥1 required approving review; consider strict up-to-date (aspects 3, 7).
6. Keep DES-0003 Draft ideation only; public kernel agnostic of out-of-tree consumers.
7. Continue pulses until ≥90; do not invent score lifts without new product evidence.
8. Consider refreshing Mac SoT working tree from GitHub `main` (still missing `.git` / onprem / aws / DES-0004+).

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0023-20260917-1339.md` | This report |
| `docs/maturity/SCORECARD.md` | Rescored (flat 80.5) |
| `docs/maturity/reports/_NEXT_FOCUS.md` | Updated for R-0023 |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-17 13:39 America/Santiago | Clark Bot | Initial report for R-0023 — flat reconfirm after R-0022 OBS+AWS lifts |
