# Hextory maturity pulse — R-0014

| Field | Value |
|---|---|
| **Report ID** | R-0014 |
| **Date / time** | 2026-09-16 19:28 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse; scheduled weekday hourly) |
| **Repo path** | Public https://github.com/marcos-blazquez/hextory @ `7efb2bd`; Cloud Agent `/workspace` |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0013](R-0013-20260916-1826.md) overall **68.3** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **68.3** |
| **Band** | Building foundations (40–69) — approaching operational candidate |
| **Meets ≥90?** | **NO** |
| **One-line summary** | Scheduled pulse after R-0013 land: main HEAD `7efb2bd` (PR #5 maturity docs); Actions **ci #11** completed Success on that SHA; local `ci_design_gates.py` OK; pytest **72 passed**. No new product surfaces since R-0011–R-0013 — still only `adapters/local`, no dashboards, no issue/PR templates, approvals=0; DES-0003 remains **Draft**. |

**Biggest blocker to 90+:** Multi-target deploy still thin (aspect 9 = 40 — only `adapters/local`) plus weak observability (52), thin Approved workflow SDD coverage, missing issue/PR templates, and `required_approving_review_count: 0`.

Δ vs R-0013: **0.0** (68.3 → 68.3) — flat reconfirm; same aspect scores as R-0011–R-0013; no invent lifts without new product evidence.

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0013 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **70** | 0 | DES-0001/0002 **Approved**; traveler contract published; DES-0003 bare **Draft**; README dark-factory thesis unchanged | Outsider dry-run / more Approved workflow vision (~+20) |
| 2 | Design-doc coverage | **60** | 0 | TEMPLATE + Approved vision + Approved engine + Draft Studio; **no new Approved workflow SDDs** this hour | More Approved workflow SDDs (~+30) |
| 3 | Review rigor (dual human+agent) | **55** | 0 | Two dual Approvals in `docs/design/reviews/`; PR #5 continues merge-via-doc habit; approvals still 0 on ruleset | Dual review on more SDDs + re-enable ≥1 approval (~+35) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **76** | 0 | DES-0002 §12 + live IMPL + `hextory.digital_traveler@0.1`; pytest **72 passed** reconfirmed | Fuller REQ↔TEST matrix (~+14) |
| 5 | Architecture purity (hexagonal + open graphs) | **80** | 0 | Pure `src/` + ports; LangGraph/Echo/OpenAI adapter-only; open `GraphRegistry`; only `adapters/local` | Second deploy adapter without core fork (~+10) |
| 6 | Determinism, testability & readable core | **84** | 0 | `tests/{unit,behavior,adapters}`; GWT BDD-style not Cucumber; CI bans cucumber; **72 passed** | More workflow coverage (~+6) |
| 7 | Automation of design gates (CI/process) | **84** | 0 | Gatekeeper + Q-GATE-1 + local gates OK; ruleset required checks; **ci #11** completed Success on `7efb2bd` ([run 35152948585](https://github.com/marcos-blazquez/hextory/actions/runs/35152948585)); approvals=0 / not strict | Re-enable ≥1 approval + strict up-to-date (~+6) |
| 8 | Factory observability (traces, quality loops) | **52** | 0 | FileCheckpointer / FileIdempotency under `.hextory/`; **no** dashboards / central metrics | Dashboards + gate-denial metrics (~+38) |
| 9 | Multi-target deploy readiness | **40** | 0 | Still `adapters/local` only; dual runtime ≠ second deploy target | Second target + parity matrix (~+50) |
| 10 | Public project readiness | **82** | 0 | Public repo + MIT + README + CONTRIBUTING; **v0.1.0**; ruleset; traveler@0.1; PRs #1–#5; **no** issue/PR templates | Templates + CoC/security + required approvals (~+8) |

**Readable code bar (cross-cutting):** No product code changed this pulse; prior interceptor/LLM ports remain walk-through-friendly.

**Sum / overall:** 70+60+55+76+80+84+84+52+40+82 = **683** ÷ 10 = **68.3**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Strong (stable)** | Required checks + PR #5 practice; approvals still 0. Not sole product goal — ceilings are multi-target + observability. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Strong (first-slice)** | `GraphRegistry` + LangGraph-from-definition; no closed-catalog regression. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Strong / location in use** | `tests/{unit,behavior,adapters}`; 72 green reconfirmed. |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Policy + runtime + local CI + hosted design-gates (required on main).** DES-0003 Draft → no UI/`adapters/web`. |
| Human merge approval = design-doc readiness (not code walkthrough) | **Documented + PR habit (#1–#5)**; ruleset requires PR + status checks; **required_approving_review_count: 0**. |
| Any merge/implementation without Approved SDD? | Engine slice under Approved DES-0002. Public tree at `7efb2bd`. This pulse: docs/maturity only. |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Inventing score lifts from PR #5 / ci #11 alone | High | **Mitigated** | Flat 68.3; merge of maturity report ≠ product maturity. |
| Treating dual runtime as multi-target | Med | **Accepted (no lift)** | Aspect 9 stays 40; only `adapters/local` on disk. |
| Claiming issue/PR templates or dashboards exist | High | **Mitigated** | `.github/` has `workflows/ci.yml` only; no ISSUE_TEMPLATE / PR template; no dashboard artifacts. |
| Naming out-of-tree consumers in public kernel docs | High | **Mitigated** | Public kernel stays agnostic; no private-sibling product names in this pulse; refer only to downstream/out-of-tree consumers. |
| Scoring code-review avoidance as sole purpose | Med | **Mitigated** | Biggest blocker framed as multi-target + observability, not “more ruleset.” |
| Closed hard-coded graph catalog regression | Med | **Mitigated** | Still open registry; no closed catalog added. |
| Cucumber-only / wrong testing-layer assumption | Low | **Mitigated** | Ordinary GWT tests; cucumber banned in CI. |

---

## 5. Learning for next run

- Flat pulses after a large lift are correct — do not inflate for “another green CI on docs.”
- Prefer public `main` as evidence SoT for pulse scoring.
- Aspect 9/8 remain hard ceilings until a second deploy adapter and dashboards land (product work, not pulse invent).
- Keep `required_approving_review_count: 0` as a soft cap on aspects 3/7.
- Re-run `python scripts/ci_design_gates.py` + pytest every pulse; expect ~72 with langgraph.
- If overall < 90: continue weekday hourly pulses; if ≥ 90: pause until human restart.
- Do not implement product features or draft new SDDs during pulse.

---

## 6. Next actions (ordered)

1. Second deploy adapter beyond `adapters/local` with traveler/Gatekeeper parity (aspect 9).
2. Observability: dashboards / structured gate-denial metrics (aspect 8).
3. Issue/PR templates for SDD contribution path (aspect 10).
4. More Approved workflow SDDs under dual review (aspects 2, 3).
5. When a second reviewer exists: re-enable ≥1 required approving review; consider strict up-to-date (aspects 3, 7).
6. Keep DES-0003 Draft ideation only; public kernel agnostic of out-of-tree consumers.
7. Continue pulses until ≥90; do not invent score lifts without new product evidence.

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0014-20260916-1928.md` | This report |
| `docs/maturity/SCORECARD.md` | Rescored to R-0014 overall **68.3** (Δ0) |
| `docs/maturity/reports/_NEXT_FOCUS.md` | Flat reconfirm after PR #5 / ci #11 |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-16 19:28 America/Santiago | Clark Bot | Scheduled R-0014 — flat reconfirm after R-0013 / PR #5 / ci #11 |
