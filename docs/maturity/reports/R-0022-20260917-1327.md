# Hextory maturity pulse — R-0022

| Field | Value |
|---|---|
| **Report ID** | R-0022 |
| **Date / time** | 2026-09-17 13:27 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse; automation) |
| **Repo path** | Public https://github.com/marcos-blazquez/hextory @ `14c4521`; box `/workspace/hextory-main-pulse` (synced `origin/main`); Mac SoT may be stale — product evidence from GitHub + box |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0021](R-0021-20260917-1245.md) overall **76.4** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **80.5** |
| **Band** | Operational dark factory (candidate) (70–89) |
| **Meets ≥90?** | **NO** |
| **One-line summary** | Product landings since R-0021: DES-0006 OBS PR **#16** @ `884f660` (MetricsPort, on-prem `GET /metrics`, local dump, Grafana JSON, TEST-OBS) + DES-0005 AWS PR **#18** @ `14c4521` (Lambda+DynamoDB/moto, uneployed SAM, LocalStack Compose). Aspects **8** 52→**74**, **9** 70→**78**; traceability/arch/tests lift modestly. Tip CI **#41** Success on `14c4521`; product OBS CI **#36** on `884f660`. Pytest **112 passed / 1 skipped**. |

**Biggest blocker to 90+:** Observability is first-slice only (aspect 8 = **74**) — MetricsPort/`/metrics`/Grafana-as-code land, but not production-grade ops (alerts, operated dashboards, traveler-journey analytics). AWS third target is moto/LocalStack parity without real-account deploy (aspect 9 = **78**). Still thin Approved *workflow* SDDs; no issue/PR templates; `required_approving_review_count: 0`.

Δ vs R-0021: **+4.1** (76.4 → 80.5). Lifts scored from product commits only — not from docs-only CI.

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0021 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **76** | 0 | DES-0001/0002/0004/0005/0006 **Approved**; DES-0003 bare **Draft**; OBS+AWS now evidenced in tree but vision docs unchanged | Outsider dry-run / Approved workflow vision (~+14) |
| 2 | Design-doc coverage | **76** | 0 | TEMPLATE + five Approved SDDs; DES-0003 Draft; **still no Approved workflow SDDs**; Approvals already credited in R-0021 | More Approved *workflow* SDDs (~+14) |
| 3 | Review rigor (dual human+agent) | **70** | 0 | Five dual Approvals in `docs/design/reviews/`; PR #16/#18 merge under Approved DES-0006/0005; `required_approving_review_count: 0` still caps | More duals + re-enable ≥1 approval (~+20) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **86** | +6 | DES-0006 ↔ `src/ports/metrics.py` + `adapters/onprem/prometheus_metrics.py` + TEST-OBS-01…08; DES-0005 ↔ `adapters/aws/*` + TEST-AWS-01…08; traveler@0.1; pytest **112 passed / 1 skipped** | Broader REQ↔TEST / workflow SDD links (~+4) |
| 5 | Architecture purity (hexagonal + open graphs) | **88** | +3 | Pure `src/` + ports; `adapters/{local,onprem,aws}`; MetricsPort Protocol (no prometheus in `src/`); TEST-AWS-07 / TEST-OBS-01 import purity; open GraphRegistry | Residual adapter-surface polish (~+2) |
| 6 | Determinism, testability & readable core | **88** | +2 | `tests/{unit,behavior,adapters}`; GWT BDD-style not Cucumber; OBS+AWS suites green; **112 passed / 1 skipped** | Formal testing-layer SDD + fuller edge coverage (~+2) |
| 7 | Automation of design gates (CI/process) | **85** | 0 | Gatekeeper + Q-GATE-1 + `ci_design_gates.py` OK (6 docs); CI installs `.[dev,onprem,aws]`; tip **ci #41** Success on `14c4521`; ruleset still approvals=0 / not strict | Re-enable ≥1 approval + strict up-to-date (~+5) |
| 8 | Factory observability (traces, quality loops) | **74** | +22 | **MetricsPort** (`src/ports/metrics.py`: gate_denials / runs_accepted / terminal / quality_fail / rework); on-prem **GET /metrics** (`adapters/onprem/prometheus_metrics.py` + `app.py`); local dump (`adapters/local/metrics_dump.py`); Grafana-as-code (`adapters/onprem/observability/grafana-factory-observability.json`, 4 panels); TEST-OBS suite; gateway wired via SafeMetrics. **Not** 85+: no operated alerts/prod scrape, no OTel/distributed traveler journey store beyond checkpointer | Production ops maturity + journey analytics (~+16) |
| 9 | Multi-target deploy readiness | **78** | +8 | **Three** targets: `adapters/local` + `adapters/onprem` + **`adapters/aws`** (Lambda handlers, DynamoDB checkpointer/idempotency, moto parity TEST-AWS-01…08, LocalStack Compose, uneployed `template.yaml`). README + TEST-AWS-08: **no real AWS account deploy**. Not fully production multi-target | Real-account / operated deploy path or stronger parity matrix (~+12) |
| 10 | Public project readiness | **84** | 0 | Public repo + MIT + README + CONTRIBUTING; **v0.1.0**; ruleset; traveler@0.1; PRs through #18; **no** issue/PR templates (`.github/` = `workflows/ci.yml` only) | Templates + CoC/security + required approvals (~+6) |

**Readable code bar (cross-cutting):** MetricsPort / Prometheus adapter / AWS handlers are walk-through-friendly Protocols + thin adapters; core stays free of boto3/prometheus.

**Sum / overall:** 76+76+70+86+88+88+85+74+78+84 = **805** ÷ 10 = **80.5**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Strong (stable)** | Product PRs #16/#18 cite Approved DES-0006/0005; not the sole product goal. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Strong (first-slice)** | GraphRegistry unchanged; OBS/AWS do not introduce closed catalogs. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Strong / location in use** | TEST-OBS + TEST-AWS GWT suites; cucumber banned; 112 green / 1 skipped. |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Policy + runtime + CI.** DES-0005/0006 **Approved** → AWS + OBS first slices **implemented** on main (`884f660`, `14c4521`). DES-0003 Draft → no UI. |
| Human merge approval = design-doc readiness (not code walkthrough) | **Documented + PR habit** (#16, #18); ruleset requires status checks; **required_approving_review_count: 0**. |
| Any merge/implementation without Approved SDD? | No — OBS under DES-0006; AWS under DES-0005; engine under DES-0002; on-prem under DES-0004. This pulse: maturity docs only. |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Scoring aspect 8 as 85+ from first-slice MetricsPort alone | High | **Mitigated** | Score **74** — evidenced instruments + scrape + Grafana JSON + tests; not production ops. |
| Scoring aspect 9 as production multi-target from moto without account deploy | High | **Mitigated** | Score **78** — third target present; README/TEST-AWS-08 forbid real-account deploy. |
| Crediting docs-only lifts for OBS/AWS (R-0021 trap inverted) | High | **Mitigated** | Product SHAs `884f660` / `14c4521` verified; tree paths + pytest counts. |
| Confusing tip CI with product CI | Med | **Mitigated** | Tip **ci #41** Success @ `14c4521` (AWS product); also cite OBS **ci #36** @ `884f660`; badge `ci - passing`. |
| prometheus_client / boto3 leaking into `src/` | High | **Mitigated** | TEST-OBS-01 + TEST-AWS-07 + `test_imports_no_adapters` green locally. |
| Inventing Mac SoT product progress from stale tree | High | **Mitigated** | Evidence from GitHub `14c4521` + box `/workspace/hextory-main-pulse`. |
| Thin Approved *workflow* SDD coverage | Med | **Open** | Five Approved platform SDDs; DES-0003 Draft; no workflow duals. |
| Grafana JSON present but never operated / scraped in prod | Med | **Open** | Counts toward first-slice OBS, not 85+ ops bar. |
| Naming out-of-tree consumers in public maturity artifacts | High | **Mitigated** | Public kernel agnostic; this report names only public hextory paths. |
| Treating code-review avoidance as sole product purpose | Med | **Mitigated** | Next actions: deepen OBS ops + AWS deploy readiness + workflow SDDs + templates. |

---

## 5. Learning for next run

- Product OBS (`884f660` / PR #16) and AWS (`14c4521` / PR #18) are on main — aspect **8=74**, **9=78**; do **not** invent further lifts without operated dashboards/alerts or real-account (or stronger operated) deploy evidence.
- Expect pytest **~112 passed / 1 skipped** with `.[dev,onprem,aws]`; tip CI installs aws extra (ci #41).
- Aspect 8 ceiling to 85+ needs production-grade ops maturity beyond MetricsPort + JSON dashboard-as-code.
- Aspect 9 ceiling needs more than moto/LocalStack uneployed SAM for “production multi-target.”
- Aspects 2/3 held — Approvals already scored in R-0021; need *workflow* SDDs + required approvals for further lift.
- Prefer GitHub `main` + box `/workspace/hextory-main-pulse` when Mac SoT is stale.
- Public kernel stays agnostic of out-of-tree consumers; DES-0003 Draft only.
- If overall < 90: continue pulses; if ≥ 90: pause until human restart.
- Do not implement product features or draft new SDDs during pulse.

---

## 6. Next actions (ordered)

1. Deepen observability ops maturity beyond first slice (operated scrape/alerts / traveler-journey visibility) — aspect 8 toward 85+.
2. Strengthen AWS multi-target path without claiming real-account deploy prematurely (parity matrix, LocalStack smoke CI optional, clear NG1) — aspect 9.
3. Issue/PR templates for SDD contribution path (aspect 10).
4. More Approved *workflow* SDDs under dual review (aspects 2, 3).
5. When a second reviewer exists: re-enable ≥1 required approving review; consider strict up-to-date (aspects 3, 7).
6. Keep DES-0003 Draft ideation only; public kernel agnostic of out-of-tree consumers.
7. Continue pulses until ≥90; do not invent score lifts without new evidence.
8. Refresh Mac SoT from GitHub `main` when practical.

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0022-20260917-1327.md` | This report |
| `docs/maturity/SCORECARD.md` | Rescored → overall **80.5** |
| `docs/maturity/reports/_NEXT_FOCUS.md` | Sticky focus for R-0023+ |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-17 13:27 America/Santiago | Clark Bot | Initial report R-0022 after OBS #16 + AWS #18 on main |
