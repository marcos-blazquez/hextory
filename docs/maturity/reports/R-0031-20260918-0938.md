# Hextory maturity pulse — R-0031

| Field | Value |
|---|---|
| **Report ID** | R-0031 |
| **Date / time** | 2026-09-18 09:38 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse; automation) |
| **Repo path** | Public https://github.com/marcos-blazquez/hextory @ `1a0d940` (tip = R-0030 maturity docs PR **#30**); product landmarks unchanged: OBS `884f660`, AWS smoke path `7b98737`, DescribeTable fix `65a46c8`; box `/workspace/hextory-pulse-r31` @ `1a0d940`; **Mac SoT offline this run** — product evidence from GitHub `main` + pulse clone only |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0030](R-0030-20260918-0824.md) overall **80.9** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **80.9** |
| **Band** | Operational dark factory (candidate) (70–89) |
| **Meets ≥90?** | **NO** |
| **One-line summary** | Flat reconfirm after R-0030. No new product commits on `main` since tip `1a0d940` (R-0030 land only; product still `65a46c8` / AWS-SMOKE-001 + DescribeTable). Tip CI **#65** Success on `1a0d940` (docs); product CI still **#63** on `65a46c8`. Local design-gates OK (6 docs); pytest **115 passed / 1 skipped**; AWS-SMOKE metrics scrape still **deferred**; `.github/` still workflows-only (no issue/PR templates). |

**Biggest blocker to 90+:** Observability remains first-slice (aspect **8 = 74**) — MetricsPort + `/metrics` + Grafana-as-code land, but not production-grade ops (alerts, operated scrape, traveler-journey analytics); AWS-SMOKE metrics scrape **deferred**. AWS third target has bounded real-account DynamoDB smoke (aspect **9 = 82**) but **no full SAM/HTTP API deploy**. Thin Approved *workflow* SDDs; no issue/PR templates; `required_approving_review_count: 0`.

Δ vs R-0030: **0.0** (80.9 → 80.9). Honest flat reconfirm — maturity tip / CI on docs alone are not a lift. First flat hour after R-0030's aspect-9 move.

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0030 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **76** | 0 | DES-0001/0002/0004/0005/0006 **Approved** (`config/sdd_status.json`); DES-0003 bare **Draft**; no vision-doc change this hour | Outsider dry-run / Approved workflow vision (~+14) |
| 2 | Design-doc coverage | **76** | 0 | TEMPLATE + five Approved SDDs; DES-0003 Draft; **still no Approved workflow SDDs** | More Approved *workflow* SDDs (~+14) |
| 3 | Review rigor (dual human+agent) | **70** | 0 | Five dual Approvals in `docs/design/reviews/`; PR #16/#18/#28/#29/#30 under Approved DES / docs path; ruleset `required_approving_review_count: 0` caps | More duals + re-enable ≥1 approval (~+20) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **86** | 0 | DES-0004/0005/0006 live IMPL + TEST-ONP/TEST-AWS/TEST-OBS; traveler@0.1; evidence note `docs/maturity/evidence/AWS-SMOKE-001.md`; pytest **115 passed / 1 skipped** on pulse clone @ `1a0d940` | Broader REQ↔TEST / workflow SDD links (~+4) |
| 5 | Architecture purity (hexagonal + open graphs) | **88** | 0 | Pure `src/` + ports; `adapters/{local,onprem,aws}`; MetricsPort Protocol (no prometheus/boto in `src/` — rg clean); open GraphRegistry | Residual adapter-surface polish (~+2) |
| 6 | Determinism, testability & readable core | **88** | 0 | `tests/{unit,behavior,adapters}`; GWT BDD-style not Cucumber; CI bans cucumber/behave/pytest-bdd; OBS+AWS suites; **115 passed / 1 skipped** | Formal testing-layer SDD + fuller edge coverage (~+2) |
| 7 | Automation of design gates (CI/process) | **85** | 0 | Gatekeeper + Q-GATE-1 + `ci_design_gates.py` OK (6 docs); tip **ci #65** Success on `1a0d940` (R-0030 maturity land); product **ci #63** on `65a46c8`; smoke docs **ci #61** on `7b98737`; ruleset still approvals=0 / not strict | Re-enable ≥1 approval + strict up-to-date (~+5) |
| 8 | Factory observability (traces, quality loops) | **74** | 0 | MetricsPort (`src/ports/metrics.py`); on-prem GET `/metrics`; local dump; Grafana JSON; TEST-OBS; gateway SafeMetrics. AWS-SMOKE-001 explicitly **defers metrics scrape**. **Not** 85+: no operated alerts/prod scrape; no OTel/journey store beyond checkpointer | Production ops maturity + journey analytics (~+16) |
| 9 | Multi-target deploy readiness | **82** | 0 | Three targets: local + onprem + **aws** (Lambda/DynamoDB/moto, LocalStack Compose, uneployed SAM). Prior: authorized AWS-SMOKE-001 **SUCCEEDED** + DescribeTable `ensure_table` (PR #28/#29). Still **no full SAM/HTTP API deploy**; CI remains moto-only (NG1). No new multi-target evidence this hour | Full SAM/HTTP operated path or stronger continuous multi-target proof (~+8) |
| 10 | Public project readiness | **84** | 0 | Public repo + MIT + README + CONTRIBUTING; **v0.1.0**; ruleset active; traveler@0.1; PRs through **#30**; **no** issue/PR templates (`.github/` = `workflows/ci.yml` only) | Templates + CoC/security + required approvals (~+6) |

**Readable code bar (cross-cutting):** No new core/adapter code this hour; MetricsPort / Prometheus / AWS handlers remain Protocols + thin adapters.

**Sum / overall:** 76+76+70+86+88+88+85+74+82+84 = **809** ÷ 10 = **80.9**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Strong (stable)** | Dual Approve + merge-via-doc habit; ceilings remain OBS ops + fuller AWS path, not more ruleset alone. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Strong (first-slice)** | `GraphRegistry` + LangGraph-from-definition; behavior test open-registry GWT; no closed-catalog regression; three adapters do not introduce fixed catalogs. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Strong / location in use** | `tests/{unit,behavior,adapters}`; ordinary GWT; cucumber banned in `ci_design_gates.py`; 115 green / 1 skipped. Location formally TBD but in use. |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Policy + runtime + local CI + hosted design-gates (required on main).** DES-0005/0006 **Approved** → AWS + OBS first slices **implemented**; Q-AWS-6 smoke amendment on DES-0005; DES-0003 Draft → no UI. |
| Human merge approval = design-doc readiness (not code walkthrough) | **Documented + PR habit**; ruleset requires PR + status checks; **required_approving_review_count: 0**. |
| Any merge/implementation without Approved SDD? | Engine under DES-0002; on-prem under DES-0004; OBS under DES-0006 (PR #16); AWS under DES-0005 (PR #18 + follow-ons #28/#29). This pulse: maturity docs only (R-0031). |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Inventing score lift from R-0030 tip / ci #65 alone | High | **Mitigated** | Tip is docs-only `1a0d940`; held all aspects flat at R-0030 levels. |
| Inventing aspect 8 lift while metrics scrape still deferred | High | **Mitigated** | Held 74; AWS-SMOKE-001 still lists metrics scrape deferred; no operated alerts. |
| Over-claiming aspect 9 from prior table-only smoke as full deploy | High | **Mitigated** | Held 82; full SAM/HTTP API **not** deployed; CI still moto (NG1). |
| Confusing tip CI #65 (maturity docs) with product CI | Med | **Mitigated** | Tip ci #65 on `1a0d940` (R-0030 land); product tip still `65a46c8` / ci #63; also cite smoke #61 / OBS #36 / first AWS #41. |
| Inventing Mac SoT product progress while Mac offline | High | **Mitigated** | Mac offline this run; evidence exclusively GitHub `main` + `/workspace/hextory-pulse-r31`. |
| Thin Approved *workflow* SDD coverage | Med | **Open** | Five Approved platform SDDs; DES-0003 Draft; no workflow SDDs dual-reviewed. |
| Closed hard-coded graph catalog regression | Med | **Mitigated** | Open registry unchanged; three adapters; `tests/behavior/test_open_registry.py` GWT. |
| Cucumber-only / wrong testing-layer assumption | Low | **Mitigated** | Ordinary GWT; cucumber/behave/pytest-bdd banned; 115/1. |
| Treating code-review avoidance as sole product purpose | Med | **Mitigated** | Next actions prioritize OBS ops + fuller AWS path, not ruleset alone. |
| Naming out-of-tree consumers in public maturity artifacts | High | **Mitigated** | Public kernel stays agnostic; this report names only public hextory paths. |
| Flat-hour after lift: mistaking diligence for unjustified Δ | Med | **Mitigated** | R-0030 had real AWS smoke evidence; this hour has none new — Δ0 required. |
| Ruleset vs classic branch-protection API | Low | **Accepted** | Classic `branches/main/protection` historically 404; active ruleset still enforces required checks + PR (`required_approving_review_count: 0`, strict=false). |
| gh CLI unauthenticated + public API rate-limit on box | Low | **Mitigated** | SSH as `mafiurs` for push; Actions run IDs/titles via public HTML; local gates + pytest green. |
| Stale box mirror `/workspace/hextory` missing product tree | Med | **Mitigated** | Pulse clone is product SoT; do not use mirror for product evidence. |

---

## 5. Learning for next run

- R-0030 lifted to **80.9** on aspect 9 only (AWS-SMOKE-001 + DescribeTable); **R-0031** is the first flat reconfirm after that lift (Δ0).
- Always separate **tip CI** (now docs #65 on `1a0d940`) from product landmarks (`65a46c8` / #63; smoke `7b98737` / #61; first AWS `14c4521` / #41; OBS `884f660` / #36).
- Aspect 8 ceiling until operated scrape/alerts/traveler-journey analytics beyond first-slice MetricsPort/`/metrics` — smoke note still defers metrics.
- Aspect 9: NG1 still forbids CI real-account deploy; authorized manual `hextory-*` smoke is in scope (Q-AWS-6); do not claim full SAM/HTTP production from table+handler smoke alone.
- Mac SoT was **offline** this hour — prefer GitHub `main` + box `/workspace/hextory-pulse-r31`; never invent Mac product progress.
- Re-run `python scripts/ci_design_gates.py` + pytest every pulse; expect ~**115 passed / 1 skipped** with `[dev,onprem,aws]` until deeper OBS/AWS tests land.
- Public kernel stays agnostic of out-of-tree consumers; DES-0003 Draft ideation only.
- If overall < 90: continue pulses; if ≥ 90: pause until human restart.
- Do not implement product features or draft new SDDs during pulse.
- **Memory deepen (<90):** next material moves need (a) operated OBS evidence (scrape/alerts/journeys), (b) fuller AWS path (SAM/HTTP API) without violating NG1 CI rule, (c) issue/PR templates, or (d) Approved workflow SDDs under dual review. Do not lift aspect 8 from smoke alone while metrics remain deferred. Prefer SSH/`mafiurs` when box `gh` is unauthenticated and public API is rate-limited; HTML Actions pages remain a fallback for run numbers. Before `git pull` on pulse clone, stash or clear local maturity edits. Flat hours remain scoring discipline when no new ops/deploy/templates/workflow-SDD evidence lands.

---

## 6. Next actions (ordered)

1. Deepen observability beyond first slice (operated scrape/alerts / traveler-journey visibility) — aspect 8 toward 85+.
2. Strengthen AWS multi-target path beyond table+handler smoke (fuller SAM/HTTP evidence) while keeping NG1 (no CI real-account deploy) clear — aspect 9 toward 85+.
3. Add issue/PR templates for SDD contribution path (aspect 10).
4. More Approved *workflow* SDDs under dual review (aspects 2, 3).
5. When a second reviewer exists: re-enable ≥1 required approving review; consider strict up-to-date (aspects 3, 7).
6. Keep DES-0003 Draft ideation only; public kernel agnostic of out-of-tree consumers.
7. Continue pulses until ≥90; do not invent score lifts without new product evidence.
8. Land this R-0031 maturity trio on `main` via PR (R-0030 already on main via #30).
9. When Mac returns online: refresh SoT working tree from GitHub `main`.

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0031-20260918-0938.md` | Created (this report) |
| `docs/maturity/SCORECARD.md` | Rescored / flat reconfirm 80.9 |
| `docs/maturity/reports/_NEXT_FOCUS.md` | Updated for R-0031 |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-18 09:38 America/Santiago | Clark Bot (maturity pulse) | Initial report for R-0031 — flat reconfirm 80.9 (Δ0 vs R-0030) |
