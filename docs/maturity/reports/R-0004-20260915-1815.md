# Hextory maturity pulse — R-0004

| Field | Value |
|---|---|
| **Report ID** | R-0004 |
| **Date / time** | 2026-09-15 18:15 America/Santiago |
| **Scorer** | Clark Bot (maturity pulse) |
| **Repo path** | `/Users/mblazquez/Documents/dev/pixpod/hextory` |
| **Method** | [ASPECTS.md](../ASPECTS.md); scores 0–100; overall = mean |
| **Template** | [REPORT_TEMPLATE.md](../REPORT_TEMPLATE.md) |
| **Prior pulse** | [R-0003](R-0003-20260915-1724.md) overall **24.7** |
| **90+ bar** | Overall ≥ 90 → pause further pulses unless human restarts |

---

## 1. Executive verdict

| Metric | Value |
|---|---|
| **Overall maturity** | **35.3** |
| **Band** | Ideation (< 40) |
| **Meets ≥90?** | **NO** |
| **One-line summary** | DES-0001 and DES-0002 are both **Approved** (dual Marcos + Clark Bot; §13 green; Q-GATE-1 interim fail-closed); first-slice `src/` + `adapters/local` + `tests/` is design-gated green but **not started**; no CI/git/CONTRIBUTING; still ideation. |

**Biggest blocker to 90+:** Execute the authorized DES-0002-J first slice under TDD/BDD-style (`tests/{unit,behavior,adapters}`) without creeping into deferred on-prem/AWS — plus CONTRIBUTING + git/CI so design gates become automatic.

Δ vs R-0003: **+10.6** overall (24.7 → 35.3), driven by DES-0002 dual Approval (coverage, rigor, traceability, architecture/test contracts on paper). Code/CI aspects stay low — **no** `src/`, `adapters/`, `tests/`, or `config/` on disk.

---

## 2. Aspect scores

| # | Aspect | Score | Δ vs R-0003 | Evidence (paths / facts) | Gap to 90 for this aspect |
|---|---|---|---|---|---|
| 1 | Vision clarity | **60** | +3 | DES-0001 **Approved**; DES-0002 **Approved**; README status line accurate; layout tree still labels 0002 as Draft; not outsider-tested | External dry-run + README tree hygiene (~+30) |
| 2 | Design-doc coverage | **55** | +15 | TEMPLATE + Approved vision + Approved `docs/design/0002-factory-engine.md` (~610 lines, DES-0002-A…J); intent non-gating; no other concrete workflow SDDs | First workflow SDDs beyond engine (~+35) |
| 3 | Review rigor (dual human+agent) | **50** | +15 | Second dual Approval: `reviews/DES-0002-agent-review-20260915.md` + SDD dual-review record; §13 all `[x]`; still only two Approvals total | Repeatable review on more SDDs (~+40) |
| 4 | Traceability (REQ↔DES↔IMPL↔TEST) | **40** | +15 | DES-0002 §12 maps REQ-0001–0004 + REQ-0010–0017 → DES-* → TEST-0010–0023; **no IMPL/TEST files** | Live IMPL/TEST links in first slice (~+50) |
| 5 | Architecture purity (hexagonal + open graphs) | **32** | +12 | DES-0002-A/C Approved (registry, not closed catalog); package layout specified; **no `src/`** — purity unproven in code | Pure core + arch/import tests (~+58) |
| 6 | Determinism, testability & readable core | **28** | +16 | DES-0002-E Approved (`tests/{unit,behavior,adapters}`; not Cucumber); DES-0002-F readable bar; **zero tests/core** | TDD/BDD-style suite + readable `src/` (~+62) |
| 7 | Automation of design gates (CI/process) | **10** | +6 | Gatekeeper + Q-GATE-1 interim (fail closed; frontmatter and/or `config/sdd_status`); still **no `.git`**, `.github`, branch protection | CI + SDD-citation + human merge-via-doc (~+80) |
| 8 | Factory observability (traces, quality loops) | **18** | +10 | DigitalTraveler + append-only `routing_history` + rework max 3 frozen in Approved SDD; no runtime traces | Live traveler traces + FAIL reports (~+72) |
| 9 | Multi-target deploy readiness | **20** | +10 | DES-0002-J authorizes `adapters/local` only; on-prem/AWS deferred; **zero adapter packages** | Local CLI+MemorySaver smoke then parity (~+70) |
| 10 | Public project readiness | **40** | +4 | MIT + README Approved labels; still **no CONTRIBUTING**, no git remote, no issue/PR templates; README tree Draft stale | Hosting + CONTRIBUTING + templates (~+50) |

**Readable code bar (cross-cutting):** Still N/A for product code (none present). DES-0002-F remains the walk-through-friendly contract for future `src/`.

**Sum / overall:** 60+55+50+40+32+28+10+18+20+40 = **353** ÷ 10 = **35.3**

---

## 3. Capability lenses

| Lens | Status this run | Note |
|---|---|---|
| Code-review avoidance (one capability, not the whole product) | **Partial** | DES-0002-D / AC-12 / TEST-0023 Approved; README merge gate clear. Not sole purpose. Still unpracticed on real implementation merges. |
| Open-ended workflow graphs (unbounded node/edge composition) | **Partial** | **DES-0002-C** + AC-07 + TEST-0018 Approved; starter ≠ closed catalog. Q-REG-1 serialization still soft. Watch amendments that collapse registry into hard-coded-only graph. |
| TDD + BDD-style (not Cucumber); testing-layer home | **Partial / location Approved** | **DES-0002-E** Approved: `tests/{unit,behavior,adapters}`; Given/When/Then in ordinary tests; Cucumber forbidden (NG3). Residual “Marcos may change in review” wording remains in body — freeze before scaffold. **No tests in repo.** |

---

## 3b. Gate & merge posture

| Check | Status |
|---|---|
| Approved SDD required before implementation | **Enforced by policy**; DES-0002 Status **Approved**, §13 all checked; first slice authorized. No product code present yet. |
| Human merge approval = design-doc readiness (not code walkthrough) | **Documented** (README, DES-0001-B, DES-0002-D) |
| Any merge/implementation without Approved SDD? | **No** — no `src/`; no `.git` on Mac SoT this run |

---

## 4. Edge cases & risks examined this run

| Edge case | Severity | Status | Note |
|---|---|---|---|
| Intent ≠ Approved (`factory-engine-intent.md` as gate pass) | High | **Mitigated in docs** | DES-0002 header: intent historical; Approved SDD is gate. Temptation remains if agents cite intent for scope. |
| Closed catalog if DES-0002-C weakened | High | Open | Registry/AC-07 must survive first-slice shortcuts (“just hard-code three nodes”). |
| Cucumber-by-mistake for “BDD” | Med | Open | DES-0002-E / NG3 / TEST-0020 forbid; easy to add Gherkin deps at scaffold. |
| Testing-layer flip before scaffold | Med | Open | Residual “Marcos may change” text in DES-0002-E section despite Approval — freeze layout before first test files. |
| Q-GATE-1 fail-closed gaps | High | **Interim recorded** | Fail closed; frontmatter and/or `config/sdd_status`; both agree if both present. No reader impl yet → false-allow risk until TEST-0011/0012. |
| First-slice scope creep into deferred adapters | High | Open | DES-0002-J / NG1: on-prem/AWS must not land in first PR. |
| Merge without SDD citation once git exists | High | Open | No CONTRIBUTING / CI yet. |
| No `.git` on Mac SoT | Med | Open | Blocks aspect 7 / public remote readiness. |
| Mac ↔ box sync drift | Med | Open | Pulse writes Mac SoT then CopyToBox → `/workspace/hextory`. |
| Score inflation from Approval alone | Med | **Mitigated this run** | Code/CI aspects stay ≤32 without `src/`/tests; overall still <40 ideation. |
| README layout tree still says DES-0002 Draft | Low | Open | Status line Correct; tree diagram stale — hygiene for next edit. |
| Dual review never repeats (one-off rigor) | Med | Improved | Two Approvals now; aspect 3 still mid until more SDDs dual-reviewed. |
| Implementing beyond Approved first slice | High | Open | Only `src/` + `adapters/local` + `tests/` authorized; pulse itself did not implement. |

---

## 5. Learning for next run

- Treat **DES-0002 Approved + §13 green** as settled: blocker is **first-slice implementation + public/CI foundations**, not drafting or dual-review of DES-0002.
- Do **not** score architecture/testability as if code exists until `src/` and `tests/` appear with evidence paths.
- Re-check closed-catalog / Cucumber / Q-GATE-1 fail-closed / scope creep every pulse until first slice ships.
- Prefer CONTRIBUTING + git init/remote + failing-first tests over cosmetic README bumps (except fix Draft-in-tree hygiene).
- If overall < 90: continue hourly weekday pulses; if ≥ 90: pause until human restart.
- Sync Mac SoT → box after every pulse write.

---

## 6. Next actions (ordered)

1. **Scaffold first slice** under DES-0002-J: readable hexagonal `src/` + `adapters/local` + TDD/BDD-style tests at `tests/{unit,behavior,adapters}` (failing tests first; no Cucumber; no on-prem/AWS).
2. Implement Gatekeeper + SddStatusReader per Q-GATE-1 interim (fail closed; frontmatter and/or `config/sdd_status`).
3. Add `CONTRIBUTING.md` encoding merge-via-doc-readiness + readable-code expectations (aspect 10).
4. Init / connect public git remote; later CI for Approved SDD citation on implementation PRs (aspect 7).
5. Fix README repository-layout tree (still says DES-0002 Draft) and define live DigitalTraveler trace emission + minimal quality FAIL report format (aspects 1, 8).
6. Re-score after first red→green core tests land (expect aspects 5/6/9 lifts; stay foundations until CI + more coverage).

---

## 7. Artifacts touched

| Path | Change |
|---|---|
| `docs/maturity/reports/R-0004-20260915-1815.md` | This report |
| `docs/maturity/SCORECARD.md` | Rescored to R-0004 |
| `docs/maturity/reports/_NEXT_FOCUS.md` | First-slice impl + CONTRIBUTING/git focus |

---

## Revision

| Date | Author | Note |
|---|---|---|
| 2026-09-15 18:15 | Clark Bot | Fourth pulse; DES-0002 Approval evidenced; overall 35.3; scoring/docs only — no factory implementation |
