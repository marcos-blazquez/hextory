"""AWS-shaped adapter — API Gateway HTTP API + Lambda + DynamoDB (DES-0005).

First slice proven with moto (unit) and optional LocalStack Compose smoke.
Never applied to a real AWS account in this slice.
"""
